# Storage Checklist

## Visible controls

- Storage page shows file count, used bytes, storage root, status filtering, CSV export, column chooser, and storage-config CRUD actions.
- Storage usage is read-only aggregate data; configuration remains server-authorized.

## Query evidence

- Fresh seeded database reports zero files, zero bytes, and storage root `/data/uploads`; the active `LOCAL` configuration matches that root.

## Responsive check

- Captures: `local-desktop.png`, `local-tablet.png`.
