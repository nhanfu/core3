# Fee Rule Checklist

## Visible controls

- Fee-rule list supports code/name search, active/inactive tabs, CSV export, column chooser, and CRUD actions.
- Rows show the configured fee expression and active state.

## Query evidence

- Fresh seeded database returned `DISTANCE` with expression `distance_km * rate`.

## Responsive check

- Captures: `local-desktop.png`, `local-tablet.png`.
