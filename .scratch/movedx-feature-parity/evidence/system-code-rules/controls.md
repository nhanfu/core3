# Code Rule Checklist

## Visible controls

- Code-rule list supports code/name search, active/inactive tabs, CSV export, column chooser, preview, and CRUD actions.
- Preview values derive from prefix, year, sequence width, reset cadence, and next sequence.

## Query evidence

- Fresh seeded database returned the `ORDER` rule with preview `DH-{YYYY}-{SEQ:4}` and active status.

## Responsive check

- Captures: `local-desktop.png`, `local-tablet.png`.
