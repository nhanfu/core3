# Trip Status Checklist

## Visible controls

- Trip-status list supports code/name search, active/inactive tabs, column chooser, and CRUD actions.
- Rows show configured value, description, and active state.

## Query evidence

- Fresh seeded database returned `SCHEDULED` with configured value `scheduled` and active status.

## Responsive check

- Captures: `local-desktop.png`, `local-tablet.png`.
