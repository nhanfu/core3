# Ledger Account Checklist

- Ledger list supports account/name search, active/inactive tabs, CSV export, column chooser, and CRUD actions.
- Fresh seeded query returned 3 accounts; account `111` is active and denominated in VND.
- Captures: `local-desktop.png`, `local-tablet.png`.
