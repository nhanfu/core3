# Approval Flow Checklist

- Flow list supports search, active/inactive tabs, CSV export, column chooser, and CRUD actions.
- Fresh seeded query returned active `ORDER_APPROVAL` with draft-to-pending-to-approved configuration.
- Captures: `local-desktop.png`, `local-tablet.png`.
