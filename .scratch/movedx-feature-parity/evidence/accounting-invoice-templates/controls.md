# Invoice Template Checklist

- Template list supports search, active/inactive tabs, CSV export, column chooser, and CRUD actions.
- Fresh seeded query returned 2 templates; `HD-DV` is active and denominated in VND.
- Captures: `local-desktop.png`, `local-tablet.png`.
