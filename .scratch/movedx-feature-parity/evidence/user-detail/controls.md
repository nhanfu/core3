# User Detail Checklist

- User detail shows profile, role assignment matrix with grant/revoke actions, effective permissions, activity timeline, chooser controls, and back navigation.
- Fresh `user-admin` query returned 6 roles, 33 effective permissions, and a valid admin profile; empty activity is rendered as an empty timeline.
- Captures: `local-desktop.png`, `local-tablet.png`.
