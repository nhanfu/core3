# Print Template Detail Checklist

- Detail shows template metadata and ordered blocks with token content, column chooser, add/edit/delete, and move actions.
- Fresh `sys-02` query returned 4 blocks, including order number and customer tokens.
- Captures: `local-desktop.png`, `local-tablet.png`.
