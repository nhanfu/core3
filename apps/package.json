{
  "name": "@core3/tms",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "PORT=3001 bun server.ts",
    "dev": "PORT=3001 bun --hot server.ts",
    "test": "cd tms && vitest run --environment jsdom",
    "audit": "bun tms/scripts/audit-pages.ts",
    "audit:ui": "bun tms/scripts/audit-ui.ts",
    "audit:workflows": "bun tms/scripts/audit-workflows.ts"
  },
  "dependencies": {
    "duckdb": "^1.1.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "jest-extended": "^4.0.0",
    "jsdom": "^25.0.0",
    "typescript": "^7.0.2",
    "vitest": "^3.0.0"
  }
}
