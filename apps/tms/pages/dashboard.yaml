title: Tổng quan

page:
  id: dashboard
  auth:
    require: [fleet.read]

datasources:
  - id: dashboard_kpis
    single: true
    permission: fleet.read
    query: |
      SELECT
        (SELECT COUNT(*) FROM trips) AS total_trips,
        (SELECT COUNT(*) FROM trips WHERE status = 'In Transit') AS in_transit,
        (SELECT COUNT(*) FROM trips WHERE status = 'Scheduled') AS scheduled,
        (SELECT COUNT(*) FROM maintenance WHERE status = 'Overdue') AS overdue_maintenance,
        (SELECT COUNT(*) FROM trucks WHERE status = 'Active') AS active_trucks
  - id: dashboard_trip_status
    single: false
    permission: fleet.read
    query: |
      SELECT CASE status
        WHEN 'Scheduled' THEN 'Đã lên lịch'
        WHEN 'In Transit' THEN 'Đang vận chuyển'
        WHEN 'Completed' THEN 'Hoàn thành'
        WHEN 'Cancelled' THEN 'Đã hủy'
        ELSE status END AS status, COUNT(*) AS trip_count
      FROM trips
      GROUP BY status
      ORDER BY trip_count DESC, status
  - id: dashboard_dispatch_queue
    single: false
    permission: fleet.read
    query: |
      SELECT t.id, t.trip_number, t.origin, t.destination,
        CASE t.status WHEN 'Scheduled' THEN 'Đã lên lịch' WHEN 'In Transit' THEN 'Đang vận chuyển' ELSE t.status END AS status,
        t.departure_time, tr.plate AS truck_plate, d.name AS driver_name
      FROM trips t
      LEFT JOIN trucks tr ON tr.id = t.truck_id
      LEFT JOIN drivers d ON d.id = t.driver_id
      WHERE t.status IN ('Scheduled', 'In Transit')
      ORDER BY CASE t.status WHEN 'In Transit' THEN 0 ELSE 1 END, t.departure_time

components:
  - type: StatRow
    source: dashboard_kpis
    stats:
      - label: Tổng số chuyến
        field: total_trips
        color: blue
      - label: Đang vận chuyển
        field: in_transit
        color: indigo
      - label: Chờ điều phối
        field: scheduled
        color: amber
      - label: Bảo dưỡng quá hạn
        field: overdue_maintenance
        color: red
      - label: Phương tiện hoạt động
        field: active_trucks
        color: green

  - type: Chart
    source: dashboard_trip_status
    title: Tổng quan trạng thái chuyến
    label_field: status
    value_field: trip_count
    width: 680
    height: 260
    color: '#4f46e5'

  - type: GridView
    source: dashboard_dispatch_queue
    page_size: 8
    columns:
      - type: TextCell
        field: trip_number
        label: Mã chuyến
      - type: TextCell
        field: origin
        label: Tuyến đường
        secondary: destination
      - type: TextCell
        field: truck_plate
        label: Phương tiện
        secondary: driver_name
      - type: BadgeCell
        field: status
        label: Trạng thái
        colors:
          Đã lên lịch: amber
          Đang vận chuyển: blue
      - type: DateCell
        field: departure_time
        label: Khởi hành
        format: short
