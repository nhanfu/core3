title: Đơn hàng

page:
  id: orders
  breadcrumb: [Quản lý, Đơn hàng]
  auth:
    require: [orders.read]

datasources:
  - id: orders
    single: false
    permission: orders.read
    query: |
      SELECT o.id, o.order_number, o.customer_name, o.customer_legal_name,
        CAST(o.order_date AS VARCHAR) AS order_date,
        COALESCE((SELECT s.status FROM order_workflow_states s WHERE s.order_id = o.id), o.status) AS status,
        CASE COALESCE((SELECT s.status FROM order_workflow_states s WHERE s.order_id = o.id), o.status)
          WHEN 'Draft' THEN 'Nháp'
          WHEN 'Pending Approval' THEN 'Đang duyệt'
          WHEN 'Approved' THEN 'Đã duyệt'
          WHEN 'Cancelled' THEN 'Đã hủy'
          ELSE o.status
        END AS status_label,
        o.shipment_type, o.route, o.transport_method, o.trip_count,
        printf('%,.0f ₫', COALESCE(o.total_amount, 0)) AS total_amount_display,
        o.created_by, CAST(o.created_at AS VARCHAR) AS created_at
      FROM orders o
      WHERE (:view_scope = 'all' OR o.branch_id = :current_branch_id)
        AND (:status IS NULL OR COALESCE((SELECT s.status FROM order_workflow_states s WHERE s.order_id = o.id), o.status) = :status)
        AND (:shipment_type IS NULL OR o.shipment_type = :shipment_type)
        AND (:transport_method IS NULL OR o.transport_method = :transport_method)
        AND (:from_date IS NULL OR o.order_date >= CAST(:from_date AS DATE))
        AND (:to_date IS NULL OR o.order_date < CAST(:to_date AS DATE) + INTERVAL 1 DAY)
        AND (:q IS NULL
          OR o.order_number ILIKE '%' || :q || '%'
          OR o.customer_name ILIKE '%' || :q || '%'
          OR o.customer_legal_name ILIKE '%' || :q || '%'
          OR o.route ILIKE '%' || :q || '%')
      ORDER BY o.order_date DESC, o.created_at DESC
  - id: order_customer_lookup
    single: false
    permission: orders.read
    query: "SELECT name AS value, code || ' - ' || name AS label FROM customers WHERE status = 'Active' AND (:view_scope = 'all' OR (:view_scope = 'branch' AND (visibility = 'Public' OR owner_name = :current_user_name)) OR (:view_scope = 'own' AND owner_name = :current_user_name)) UNION SELECT DISTINCT customer_name AS value, customer_name AS label FROM orders WHERE customer_name IS NOT NULL AND customer_name <> '' AND (:view_scope = 'all' OR branch_id = :current_branch_id) ORDER BY label"
  - id: order_shipment_type_lookup
    single: false
    permission: orders.read
    query: "SELECT name AS value, code || ' - ' || name AS label FROM system_configs WHERE kind = 'shipment_type' AND status = 'Active' UNION SELECT DISTINCT shipment_type AS value, shipment_type AS label FROM orders WHERE shipment_type IS NOT NULL AND shipment_type <> '' AND (:view_scope = 'all' OR branch_id = :current_branch_id) ORDER BY label"

toolbar:
  - id: add_order_btn
    label: '+ Thêm đơn hàng'
    variant: primary
    permission: orders.write
    action: add_order

components:
  - type: ListToolbar
    source: orders
    filter_field: q
    search:
      label: Tìm kiếm đơn hàng
      placeholder: 'Tìm mã đơn, khách hàng, tên hàng...'
    date_range: { from_field: from_date, to_field: to_date, from_label: 'Từ ngày', to_label: 'Đến ngày', presets: [today, week, month, quarter, year, all] }
    filters:
      - field: shipment_type
        label: Loại hình
        placeholder: Tất cả loại hình
        options_source: order_shipment_type_lookup
      - field: transport_method
        label: Hình thức
        placeholder: Tất cả hình thức
        options: [Đường bộ, Đường biển, Đường sắt, Hàng không]
    actions:
      - { id: orders.export, label: '', title: 'Xuất CSV', icon: download }
    help: true
  - type: StatusTabs
    source: orders
    filter_field: status
    variant: contained
    tabs:
      - { id: '', label: Tất cả }
      - { id: Draft, label: Nháp }
      - { id: Pending Approval, label: Đang duyệt }
      - { id: Approved, label: Đã duyệt }
      - { id: Cancelled, label: Đã hủy }
  - type: DataGrid
    source: orders
    page_size: 50
    page_size_options: [10, 25, 50, 100]
    row_numbers: true
    column_chooser: true
    row_key: id
    selectable: true
    empty_state:
      title: Không có đơn hàng
      description: Thử thay đổi bộ lọc hoặc tạo đơn hàng mới.
    columns:
      - { field: order_number, label: Mã HT, type: PrimaryEntityCell, sortable: true }
      - { field: customer_name, label: Khách hàng }
      - { field: order_date, label: Ngày đơn, sortable: true }
      - field: status_label
        label: Trạng thái
        type: StatusChip
        colors:
          Nháp: neutral
          Đang duyệt: amber
          Đã duyệt: green
          Đã hủy: red
      - { field: shipment_type, label: Loại hình }
      - { field: route, label: Tuyến }
      - { field: transport_method, label: Hình thức }
      - { field: trip_count, label: Số chuyến, align: right }
      - { field: total_amount_display, label: Tổng tiền, align: right }
      - { field: created_by, label: Người tạo }
      - { field: created_at, label: Ngày giờ tạo }
      - field: actions
        label: ''
        sortable: false
        align: right
        actions:
          - { id: view_order, label: Chi tiết, variant: secondary, permission: orders.read }
          - { id: edit_order, label: Sửa, variant: ghost, permission: orders.write, show_if: "row.status === 'Draft'" }
          - { id: submit_order, label: Trình duyệt, variant: primary, permission: orders.write, show_if: "row.status === 'Draft'" }
          - { id: approve_order, label: Duyệt, variant: primary, permission: orders.approve, show_if: "row.status === 'Pending Approval'" }
          - { id: reject_order, label: Từ chối, variant: ghost, permission: orders.approve, show_if: "row.status === 'Pending Approval'" }
          - { id: cancel_order, label: Hủy, variant: danger, permission: orders.write, show_if: "row.status !== 'Cancelled'" }
          - { id: delete_order, label: Xóa, variant: danger, permission: orders.write, show_if: "row.status === 'Draft'" }

actions:
  - id: add_order
    type: form
    permission: orders.write
    title: Thêm đơn hàng
    table: orders
    operation: insert
    refresh: [orders]
    fields:
      - { field: order_number, label: Mã đơn, type: text, required: true }
      - { field: customer_name, label: Khách hàng, type: async-select, options_source: order_customer_lookup, search_placeholder: 'Tìm khách hàng...', required: true }
      - { field: customer_legal_name, label: Tên pháp lý, type: text }
      - { field: order_date, label: Ngày đơn, type: date, required: true }
      - { field: shipment_type, label: Loại hình, type: select, options_source: order_shipment_type_lookup }
      - { field: route, label: Tuyến, type: text }
      - { field: transport_method, label: Hình thức, type: select, options: [Đường bộ, Đường biển, Đường sắt, Hàng không], default: Đường bộ }
      - { field: trip_count, label: Số chuyến, type: number, default: 0 }
      - { field: notes, label: Ghi chú, type: textarea }
  - id: edit_order
    type: form
    permission: orders.write
    title: Cập nhật đơn hàng
    table: orders
    operation: update
    prefill: row
    refresh: [orders]
    fields:
      - { field: order_number, label: Mã đơn, type: text, required: true }
      - { field: customer_name, label: Khách hàng, type: async-select, options_source: order_customer_lookup, search_placeholder: 'Tìm khách hàng...', required: true }
      - { field: customer_legal_name, label: Tên pháp lý, type: text }
      - { field: order_date, label: Ngày đơn, type: date, required: true }
      - { field: shipment_type, label: Loại hình, type: select, options_source: order_shipment_type_lookup }
      - { field: route, label: Tuyến, type: text }
      - { field: transport_method, label: Hình thức, type: select, options: [Đường bộ, Đường biển, Đường sắt, Hàng không] }
      - { field: trip_count, label: Số chuyến, type: number }
      - { field: notes, label: Ghi chú, type: textarea }
  - id: view_order
    type: navigate
    permission: orders.read
    navigate_to: /orders/detail
    params: { id: '{row.id}' }
  - id: submit_order
    type: server
    permission: orders.write
    action: orders.submit_for_approval
    confirm: 'Trình duyệt đơn hàng {{row.order_number}}?'
    refresh: [orders]
  - id: approve_order
    type: server
    permission: orders.approve
    action: orders.approve
    confirm: 'Duyệt đơn hàng {{row.order_number}}?'
    refresh: [orders]
  - id: reject_order
    type: server
    permission: orders.approve
    action: orders.reject
    confirm: 'Từ chối và trả đơn hàng {{row.order_number}} về trạng thái nháp?'
    refresh: [orders]
  - id: cancel_order
    type: server
    permission: orders.write
    action: orders.cancel
    confirm: 'Hủy đơn hàng {{row.order_number}}?'
    refresh: [orders]
  - id: delete_order
    type: delete
    permission: orders.write
    confirm: 'Xóa đơn hàng {{row.order_number}}?'
    table: orders
    refresh: [orders]
