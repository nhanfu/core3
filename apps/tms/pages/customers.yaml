title: Khách hàng
scope: { label: 'Phạm vi xem', value: 'Toàn công ty' }

page:
  id: customers
  breadcrumb: [Kinh doanh, Khách hàng]
  auth:
    require: [crm.read]

datasources:
  - id: customers
    single: false
    permission: crm.read
    query: |
      SELECT c.id, REPLACE(UPPER(c.id), 'CUSTOMER-', 'KH') AS system_code,
        c.code, c.name, c.tax_code, c.phone, c.email, c.stage, c.visibility, c.status,
        'Khách hàng' AS entity_type,
        CASE c.stage
          WHEN 'Lead' THEN 'Khách tiềm năng'
          WHEN 'Contacting' THEN 'Đang tiếp cận'
          WHEN 'Customer' THEN 'Khách hàng'
          ELSE c.stage
        END AS stage_label,
        c.owner_name,
        'Admin User' AS created_by,
        CASE c.visibility WHEN 'Private' THEN 'Riêng' ELSE 'Công khai' END AS visibility_label,
        CASE c.status WHEN 'Inactive' THEN 'Ngưng hoạt động' ELSE 'Hoạt động' END AS status_label,
        c.created_at
      FROM customers c
      WHERE (NULLIF(:stage, '') IS NULL OR c.stage = :stage)
        AND (NULLIF(:visibility, '') IS NULL OR c.visibility = :visibility)
        AND (NULLIF(:status, '') IS NULL OR c.status = :status)
        AND (:q IS NULL OR c.code ILIKE '%' || :q || '%'
          OR c.name ILIKE '%' || :q || '%'
          OR c.tax_code ILIKE '%' || :q || '%'
          OR c.phone ILIKE '%' || :q || '%')
      ORDER BY c.created_at DESC, c.name
  - id: customer_owner_lookup
    single: false
    permission: crm.read
    query: "SELECT name AS value, name AS label FROM employees WHERE status = 'Active' UNION SELECT DISTINCT owner_name AS value, owner_name AS label FROM customers WHERE owner_name IS NOT NULL AND owner_name <> '' ORDER BY label"

toolbar:
  - id: add_customer_btn
    label: '+ Thêm khách hàng'
    variant: primary
    permission: crm.write
    action: add_customer

components:
  - type: ListToolbar
    source: customers
    filter_field: q
    search:
      label: Tìm kiếm khách hàng
      placeholder: 'Tìm mã, tên, MST, SĐT...'
    actions: [{ id: customers.export, label: '', title: 'Xuất CSV', icon: '↓' }]
    help: true
    filters:
      - { field: visibility, label: Hiển thị, placeholder: Tất cả hiển thị, options: [{ id: Public, label: Công khai }, { id: Private, label: Riêng }] }
      - { field: status, label: Trạng thái, placeholder: Tất cả trạng thái, options: [{ id: Active, label: Hoạt động }, { id: Inactive, label: Ngưng hoạt động }] }
  - type: StatusTabs
    source: customers
    filter_field: stage
    tabs:
      - { id: '', label: Tất cả }
      - { id: Lead, label: Khách tiềm năng }
      - { id: Contacting, label: Đang tiếp cận }
      - { id: Customer, label: Khách hàng }
  - type: DataGrid
    source: customers
    page_size: 50
    page_size_options: [10, 25, 50, 100]
    row_numbers: true
    column_chooser: true
    row_key: id
    selectable: true
    empty_state:
      title: Không có khách hàng
      description: Thử thay đổi bộ lọc hoặc tạo khách hàng mới.
    columns:
      - field: system_code
        label: Mã HT
        sortable: true
      - field: code
        label: Mã / tên
        type: PrimaryEntityCell
        secondary: name
        avatar: building
      - field: entity_type
        label: Loại
        type: StatusChip
        colors:
          Khách hàng: neutral
      - field: tax_code
        label: Mã số thuế
      - field: stage_label
        label: Giai đoạn
        type: StatusChip
        colors:
          Khách tiềm năng: blue
          Đang tiếp cận: amber
          Khách hàng: green
      - field: owner_name
        label: Sale phụ trách
      - field: phone
        label: Liên hệ chính
        type: PrimaryEntityCell
        secondary: email
      - field: visibility_label
        label: Hiển thị
      - field: status_label
        label: Trạng thái
        type: StatusChip
        colors:
          Hoạt động: green
          Ngưng hoạt động: red
      - field: created_by
        label: Người tạo
      - field: created_at
        label: Ngày giờ tạo
      - field: actions
        label: ''
        sortable: false
        align: right
        actions:
          - id: view_customer
            label: Chi tiết
            variant: ghost
          - id: edit_customer
            label: Sửa
            variant: ghost
          - id: delete_customer
            label: Xóa
            variant: danger

actions:
  - { id: view_customer, type: navigate, navigate_to: /crm/entities/detail, params: { id: '{row.id}', kind: customer, back: /customers } }
  - id: add_customer
    type: form
    title: Thêm khách hàng
    table: customers
    operation: insert
    refresh: [customers]
    fields:
      - { field: code, label: Mã khách hàng, type: text, required: true }
      - { field: name, label: Tên khách hàng, type: text, required: true }
      - { field: tax_code, label: Mã số thuế, type: text }
      - { field: phone, label: Số điện thoại, type: text }
      - { field: email, label: Email, type: text }
      - { field: stage, label: Giai đoạn, type: select, options: [Lead, Contacting, Customer], default: Lead }
      - { field: owner_name, label: Sale phụ trách, type: select, options_source: customer_owner_lookup }
      - { field: visibility, label: Hiển thị, type: select, options: [Public, Private], default: Public }
      - { field: status, label: Trạng thái, type: select, options: [Active, Inactive], default: Active }
  - id: edit_customer
    type: form
    title: Cập nhật khách hàng
    table: customers
    operation: update
    prefill: row
    refresh: [customers]
    fields:
      - { field: code, label: Mã khách hàng, type: text, required: true }
      - { field: name, label: Tên khách hàng, type: text, required: true }
      - { field: tax_code, label: Mã số thuế, type: text }
      - { field: phone, label: Số điện thoại, type: text }
      - { field: email, label: Email, type: text }
      - { field: stage, label: Giai đoạn, type: select, options: [Lead, Contacting, Customer] }
      - { field: owner_name, label: Sale phụ trách, type: select, options_source: customer_owner_lookup }
      - { field: visibility, label: Hiển thị, type: select, options: [Public, Private] }
      - { field: status, label: Trạng thái, type: select, options: [Active, Inactive] }
  - id: delete_customer
    type: delete
    confirm: 'Xóa khách hàng {{row.name}}?'
    table: customers
    refresh: [customers]
