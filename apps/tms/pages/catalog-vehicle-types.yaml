title: Loại xe

page:
  id: catalog-vehicle-types
  auth:
    require: [catalog.read]

datasources:
  - id: vehicle_types
    single: false
    permission: catalog.read
    query: |
      SELECT id, code, name, description, status, sort_order, created_at
      FROM master_data
      WHERE kind = 'vehicle_type'
        AND (:status IS NULL OR status = :status)
        AND (:q IS NULL OR code ILIKE '%' || :q || '%' OR name ILIKE '%' || :q || '%' OR description ILIKE '%' || :q || '%')
      ORDER BY sort_order, code

toolbar:
  - { id: add_vehicle_type_btn, label: '+ Thêm loại xe', variant: primary, permission: catalog.write, action: add_vehicle_type }

components:
  - type: ListToolbar
    source: vehicle_types
    filter_field: q
    search: { label: Tìm kiếm loại xe, placeholder: 'Tìm mã, tên hoặc mô tả...' }
    actions: [{ id: vehicle_types.export, label: 'Xuất CSV', icon: '↓' }, { id: import_vehicle_types, label: 'Nhập CSV', icon: '↑' }]
  - type: StatusTabs
    source: vehicle_types
    filter_field: status
    tabs:
      - { id: '', label: Tất cả }
      - { id: Active, label: Đang sử dụng }
      - { id: Inactive, label: Ngưng sử dụng }
  - type: DataGrid
    source: vehicle_types
    column_chooser: true
    page_size: 20
    row_key: id
    selectable: true
    empty_state: { title: Không có loại xe, description: Tạo loại xe đầu tiên để sử dụng trong danh mục. }
    columns:
      - { field: code, label: Mã, sortable: true }
      - { field: name, label: Tên loại xe, type: PrimaryEntityCell, secondary: description }
      - { field: sort_order, label: Thứ tự, align: right }
      - field: status
        label: Trạng thái
        type: StatusChip
        colors: { Active: green, Inactive: red }
      - { field: created_at, label: Ngày tạo }
      - field: actions
        label: ''
        sortable: false
        align: right
        actions:
          - { id: edit_vehicle_type, label: Sửa, variant: ghost }
          - { id: delete_vehicle_type, label: Xóa, variant: danger }

actions:
  - { id: import_vehicle_types, type: upload, kind: master_data_import, scope: vehicle_type, refresh: [vehicle_types] }
  - id: add_vehicle_type
    type: form
    title: Thêm loại xe
    table: master_data
    scope: vehicle_type
    operation: insert
    refresh: [vehicle_types]
    fields:
      - { field: code, label: Mã, type: text, required: true }
      - { field: name, label: Tên loại xe, type: text, required: true }
      - { field: description, label: Mô tả, type: textarea }
      - { field: sort_order, label: Thứ tự, type: number, default: 0 }
      - { field: status, label: Trạng thái, type: select, options: [Active, Inactive], default: Active }
  - id: edit_vehicle_type
    type: form
    title: Cập nhật loại xe
    table: master_data
    scope: vehicle_type
    operation: update
    prefill: row
    refresh: [vehicle_types]
    fields:
      - { field: code, label: Mã, type: text, required: true }
      - { field: name, label: Tên loại xe, type: text, required: true }
      - { field: description, label: Mô tả, type: textarea }
      - { field: sort_order, label: Thứ tự, type: number }
      - { field: status, label: Trạng thái, type: select, options: [Active, Inactive] }
  - { id: delete_vehicle_type, type: delete, table: master_data, scope: vehicle_type, confirm: 'Xóa loại xe {{row.name}}?', refresh: [vehicle_types] }
