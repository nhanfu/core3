title: Giấy báo nợ
page: { id: accounting-debit-notes, auth: { require: [accounting.read] } }
datasources:
  - id: debit_notes
    single: false
    permission: accounting.read
    query: |
      SELECT id, code, name, counterparty, amount, currency,
        printf('%,.0f', COALESCE(amount, 0)) AS amount_display, status,
        CASE status WHEN 'Draft' THEN 'Nháp' WHEN 'Pending Approval' THEN 'Đang duyệt'
          WHEN 'Approved' THEN 'Đã duyệt' WHEN 'Paid' THEN 'Đã thanh toán'
          WHEN 'Cancelled' THEN 'Đã hủy' ELSE status END AS status_label,
        CAST(document_date AS VARCHAR) AS document_date, CAST(due_date AS VARCHAR) AS due_date, description, created_at
      FROM accounting_entries WHERE kind = 'debit_note' AND (:status IS NULL OR status = :status)
        AND (:q IS NULL OR code ILIKE '%' || :q || '%' OR name ILIKE '%' || :q || '%' OR counterparty ILIKE '%' || :q || '%')
      ORDER BY document_date DESC, code
  - id: debit_note_currency_lookup
    single: false
    permission: accounting.read
    query: "SELECT code AS value, code || ' - ' || name AS label FROM master_data WHERE kind = 'currency' AND status = 'Active' UNION SELECT DISTINCT currency AS value, currency AS label FROM accounting_entries WHERE currency IS NOT NULL AND currency <> '' ORDER BY label"
toolbar:
  - { id: add_debit_note_btn, label: '+ Thêm giấy báo nợ', variant: primary, permission: accounting.write, action: add_debit_note }
components:
  - { type: ListToolbar, source: debit_notes, filter_field: q, search: { label: Tìm kiếm giấy báo nợ, placeholder: 'Tìm số chứng từ hoặc đối tác...' }, actions: [{ id: debit_notes.export, label: 'Xuất CSV', icon: '↓' }] }
  - type: StatusTabs
    source: debit_notes
    filter_field: status
    tabs: [{ id: '', label: Tất cả }, { id: Draft, label: Nháp }, { id: Pending Approval, label: Đang duyệt }, { id: Approved, label: Đã duyệt }, { id: Paid, label: Đã thanh toán }, { id: Cancelled, label: Đã hủy }]
  - type: DataGrid
    source: debit_notes
    page_size: 20
    column_chooser: true
    row_key: id
    selectable: true
    empty_state: { title: Không có giấy báo nợ }
    columns:
      - { field: code, label: Số chứng từ, sortable: true }
      - { field: name, label: Nội dung, type: PrimaryEntityCell, secondary: counterparty }
      - { field: document_date, label: Ngày chứng từ }
      - { field: due_date, label: Hạn thanh toán }
      - { field: amount_display, label: Giá trị, align: right }
      - { field: currency, label: Tiền tệ }
      - field: status_label
        label: Trạng thái
        type: StatusChip
        colors: { Nháp: neutral, Đang duyệt: amber, Đã duyệt: green, Đã thanh toán: blue, Đã hủy: red }
      - field: actions
        label: ''
        sortable: false
        actions:
          - { id: view_debit_note, label: Chi tiết, variant: secondary }
          - { id: edit_debit_note, label: Sửa, variant: ghost, show_if: "row.status === 'Draft'" }
          - { id: submit_debit_note, label: Trình duyệt, variant: primary, show_if: "row.status === 'Draft'" }
          - { id: approve_debit_note, label: Duyệt, variant: primary, show_if: "row.status === 'Pending Approval' && user.permissions.includes('accounting.approve')" }
          - { id: reject_debit_note, label: Từ chối, variant: ghost, show_if: "row.status === 'Pending Approval' && user.permissions.includes('accounting.approve')" }
          - { id: pay_debit_note, label: Đã thanh toán, variant: primary, show_if: "row.status === 'Approved' && user.permissions.includes('accounting.pay')" }
          - { id: cancel_debit_note, label: Hủy, variant: danger, show_if: "['Draft', 'Pending Approval', 'Approved'].includes(row.status)" }
          - { id: delete_debit_note, label: Xóa, variant: danger, show_if: "row.status === 'Draft'" }
actions:
  - id: add_debit_note
    type: form
    title: Thêm giấy báo nợ
    table: accounting_entries
    scope: debit_note
    operation: insert
    refresh: [debit_notes]
    fields: [{ field: code, label: Số chứng từ, type: text, required: true }, { field: name, label: Nội dung, type: text, required: true }, { field: counterparty, label: Đối tác, type: text }, { field: currency, label: Tiền tệ, type: select, options_source: debit_note_currency_lookup, default: VND }, { field: document_date, label: Ngày chứng từ, type: date }, { field: due_date, label: Hạn thanh toán, type: date }, { field: description, label: Ghi chú, type: textarea }]
  - id: edit_debit_note
    type: form
    title: Cập nhật giấy báo nợ
    table: accounting_entries
    scope: debit_note
    operation: update
    prefill: row
    refresh: [debit_notes]
    fields: [{ field: code, label: Số chứng từ, type: text, required: true }, { field: name, label: Nội dung, type: text, required: true }, { field: counterparty, label: Đối tác, type: text }, { field: currency, label: Tiền tệ, type: select, options_source: debit_note_currency_lookup }, { field: document_date, label: Ngày chứng từ, type: date }, { field: due_date, label: Hạn thanh toán, type: date }, { field: description, label: Ghi chú, type: textarea }]
  - { id: view_debit_note, type: navigate, navigate_to: /accounting/documents/detail, params: { id: '{row.id}', kind: debit_note, back: /accounting/debit-notes } }
  - { id: submit_debit_note, type: server, action: accounting.debit_notes.submit_for_approval, confirm: 'Trình duyệt giấy báo nợ {{row.code}}?', refresh: [debit_notes] }
  - { id: approve_debit_note, type: server, action: accounting.debit_notes.approve, confirm: 'Duyệt giấy báo nợ {{row.code}}?', refresh: [debit_notes] }
  - { id: reject_debit_note, type: server, action: accounting.debit_notes.reject, confirm: 'Từ chối giấy báo nợ {{row.code}}?', refresh: [debit_notes] }
  - { id: pay_debit_note, type: server, action: accounting.debit_notes.mark_paid, confirm: 'Xác nhận đã thanh toán giấy báo nợ {{row.code}}?', refresh: [debit_notes] }
  - { id: cancel_debit_note, type: server, action: accounting.debit_notes.cancel, confirm: 'Hủy giấy báo nợ {{row.code}}?', refresh: [debit_notes] }
  - { id: delete_debit_note, type: delete, table: accounting_entries, scope: debit_note, confirm: 'Xóa giấy báo nợ {{row.code}}?', refresh: [debit_notes] }
