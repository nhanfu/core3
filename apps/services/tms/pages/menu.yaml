menu:
  dashboard:
    path: /orders
    label: Orders
    icon: logo
    permission: orders.read
  groups:
    - id: operations
      label: ORDERS
      items:
        - path: Orders
          label: Orders
          icon: truck
          children:
            - { path: /orders, label: Orders, icon: document, permission: orders.read }
