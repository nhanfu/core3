import { BaseComponent } from '../runtime.js';
export declare class ActionButton extends BaseComponent {
    actions: any[];
    def: {};
    constructor(id: any, state: any, actions?: any[]);
    draw(container: any): void;
}
