# Verification

The focused suite verifies source tracing, page/API `page.id` joins, durable
seeded assignments, same-company interviewer filtering, search/status/empty
results, transport errors, read-only action shape, guarded direct detail
access, and persistence of assignment rows.

The Odoo visual gate is unresolved. No authenticated Odoo DOM or screenshot
was captured because BrowserSkill could not borrow tab `1770662590`.
