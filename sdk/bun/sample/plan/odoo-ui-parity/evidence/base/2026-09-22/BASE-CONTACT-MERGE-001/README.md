# BASE-CONTACT-MERGE-001

Bounded Base/Contacts feature: Odoo partner merge wizard parity contract.

Core3 implements the Contacts list Merge bulk action, destination selector,
durable relation reparenting, source deletion, audit record, permissions, and
stale/scope/hierarchy/email guards. This is a bounded feature result; it does
not sign off the Base/Contacts module.

Browser comparison is blocked by the shared authenticated-tab borrow state.
See `browser-check.md`; no Odoo/Core3 visual-parity claim is made.
