# Test results

Command:

```text
cd sdk/bun && bun test ./sample/test/sms_marketing_utm_campaign_mailing_tab.integration.test.ts --timeout 20000
```

Result: **pass**, 4 tests, 21 expectations, 0 failures.

Coverage includes Odoo source mapping, page/API separation, deterministic
migration replay, linked mailing rows, empty state, A/B count, duplicate
persistence, parent count/version updates, active-list validation, and stale
mailing/campaign rejection.
