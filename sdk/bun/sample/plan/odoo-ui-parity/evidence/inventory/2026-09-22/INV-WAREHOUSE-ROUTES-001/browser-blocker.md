# Browser verification gap

BrowserSkill connected to shared Chrome instance `245ea108`. The signed-in
user tabs were listed, but no tab was borrowed because the user explicitly
required confirmation and then directed the task to finish without waiting for
tab confirmation. The BrowserSkill session was stopped cleanly.

No authenticated Odoo Routes form/action interaction or desktop/mobile
captures are claimed for this feature. The local Odoo source comparison and
Core3 focused contract tests remain the available evidence.
