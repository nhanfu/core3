# Verification

Core3 contract verification passed through the focused repository test. The
test uses real DuckDB migrations and the YAML repository, including a
file-backed close/reopen assertion.

Authenticated browser verification is blocked for this turn. bsk successfully
connected to browser instance `245ea108` and listed the existing user Odoo tab,
but borrowing tab `1770662590` required confirmation and remained pending until
the agent session expired. The session was then stopped/removed; no credentials,
cookies, or tokens were read. Because the authenticated Odoo tab could not be
borrowed and the shared Core3 route runtime was not available in the bounded
window, desktop/mobile screenshots and visual parity are not claimed.
