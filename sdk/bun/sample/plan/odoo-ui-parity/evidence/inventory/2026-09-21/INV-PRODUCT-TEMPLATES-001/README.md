# INV-PRODUCT-TEMPLATES-001 evidence

- Core3 authenticated actor: `admin@tms.local`
- Core3 runtime: `http://127.0.0.1:4333`
- Desktop: 1440x900 list/detail screenshots and browser result JSON
- Mobile: 390x844 list/detail screenshots and browser result JSON
- `core3-browser.json` records page/request checks and zero horizontal overflow for both viewports.
- `source-comparison.json` pairs the Odoo Products action with the Core3 page/API/migration files.
- `odoo-blocker.json` records the exact bounded HTTP probe; no Odoo mutation is claimed.
