# Units & Packagings blockers

- No paired Odoo route blocker: authenticated `/odoo/action-90` renders the
  actual Units & Packagings list at both requested viewports.
- Core3 browser capture is read-only list/detail evidence. The New control did
  not open a rendered form in the bounded desktop retry, so no browser CRUD
  claim is made; CRUD, permissions, company guards, stale writes, and restart
  persistence are covered by the focused integration suite.
- Core3 recorded an unrelated aborted `/api/v1/notifications` poll in the
  mobile context; it did not affect feature datasource requests, rendering, or
  viewport checks.

Full Inventory sign-off remains open pending the broader module actor matrix
and remaining parity surfaces.
