# INV-PACKAGE-TYPES-001 evidence

Bounded slice: Odoo Inventory > Configuration > Delivery > Package Types.

- Core3 runtime: `http://127.0.0.1:4330`, authenticated as `admin@tms.local`.
- Desktop: 1440x900 list/detail screenshots and `core3-desktop.json`.
- Mobile: 390x844 list/detail screenshots and `core3-mobile.json`.
- Core3 page/source requests returned HTTP 200, browser page errors were empty,
  and both viewports reported `horizontal_overflow: false`.
- Odoo source/menu comparison is in `source-comparison.md`.
- The paired live Odoo route was not claimable; exact redirect/blocker is in
  `odoo-blocker.json` and `blockers.md`.

This is feature evidence only. It does not claim full Inventory sign-off.
