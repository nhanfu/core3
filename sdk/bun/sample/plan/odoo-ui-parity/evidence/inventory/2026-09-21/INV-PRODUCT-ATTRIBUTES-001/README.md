# INV-PRODUCT-ATTRIBUTES-001 evidence

- Core3 authenticated actor: `admin@tms.local`
- Core3 runtime: `http://127.0.0.1:4332`
- Desktop: 1440x900 list/detail screenshots and browser result JSON
- Mobile: 390x844 list/detail screenshots and browser result JSON
- `core3-browser.json` records HTTP/page/request checks and zero horizontal overflow for both viewports.
- `source-comparison.json` pairs the Odoo menu/action/model references with the Core3 page/API/migration files.
- `odoo-blocker.json` records the exact bounded HTTP probe result; no Odoo mutation is claimed.
