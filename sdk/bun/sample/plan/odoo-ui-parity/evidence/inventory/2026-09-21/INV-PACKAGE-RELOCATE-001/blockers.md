# INV-PACKAGE-RELOCATE-001 blockers and open scope

- The live Odoo package form/action was not captured in this bounded wave;
  source XML and Python references are recorded in `source-comparison.md`.
  No Odoo mutation was attempted.
- Core3 records the package-level relocation audit and updates the package
  location. Full Odoo quant-chain movement, nested package propagation,
  destination-container rules, and bulk package-list actions remain open.
- The desktop browser run logged one benign aborted `/api/v1/companies`
  request during shell hydration; the feature page had no page errors, and the
  scoped feature requests succeeded. Mobile had no failed requests.
