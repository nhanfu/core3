# `SURVEYS-PUBLIC-QUESTION-IMAGE-001`

Bounded slice: durable Odoo-style suggested-answer image delivery and public Choice renderer binding.

Evidence: `qa-inventory.md`, `source-comparison.md`, `test-results.md`, `verification.md`, `core3-runtime-blocker.json`, `core3-{admin,public}-{desktop,mobile}.png`, `core3-browser-results.json`, `odoo-blocker.json`, `odoo-{desktop,mobile}.png`.

Core3 authenticated desktop/mobile evidence passed after the historical shared page-schema boundary was repaired: the image rendered with HTTP 200 SVG delivery, no browser failures, and no horizontal overflow. Odoo remains at the login/proxy blocker state; no parity sign-off is claimed.
