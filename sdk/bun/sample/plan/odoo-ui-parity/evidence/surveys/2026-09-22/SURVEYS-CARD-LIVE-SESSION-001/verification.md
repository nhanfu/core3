# Verification

- BrowserSkill status found connected Chrome instance `245ea108`.
- One task-created session (`rgjj`) navigated to
  `http://localhost:8069/odoo/surveys?db=core3_reference`; the page rendered
  authenticated Odoo Surveys Cards at 1916x833.
- Session `rgjj` was stopped immediately after the desktop capture. No
  credentials, cookies, tokens, or borrowed user tab were accessed.
- The Odoo reference was used for source/UI evidence only. Core3 browser
  visual verification was not claimed because the requested BrowserSkill run
  targeted the Odoo reference URL.
- Credentials: none were accessed or recorded.
