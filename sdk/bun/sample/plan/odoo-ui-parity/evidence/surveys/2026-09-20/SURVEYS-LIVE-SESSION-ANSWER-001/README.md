# SURVEYS-LIVE-SESSION-ANSWER-001

Bounded parity slice: an attendee submits one answer for the host's current
live-session question. The Core3 page and API contracts remain separate and
join through `page.id: survey-live-session-join`.

Core3 evidence is authenticated and was captured from the isolated runtime at
`http://127.0.0.1:4016` at 1440x900 and 390x844. Odoo evidence is authenticated
against `http://127.0.0.1:8069` at the same desktop/mobile boundary. The Odoo
session-code fixture is unavailable; the exact response is recorded in
`odoo-browser-results.json`, so this is conditional evidence and not parity
sign-off.
