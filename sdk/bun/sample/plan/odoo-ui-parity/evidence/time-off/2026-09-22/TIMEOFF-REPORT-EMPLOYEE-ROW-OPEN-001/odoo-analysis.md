# Odoo analysis

Source: `/home/nhanjs/projects/odoo/addons/hr_holidays/views/hr_leave_views.xml`.

The installed `action_hr_available_holidays_report` is named `Time Off by
Employee`, targets `hr.leave`, and declares
`list,graph,pivot,calendar,form`. Therefore a report row represents a durable
leave request and can open the standard leave form. The source action is
read-only at the report level; the destination form supplies the existing
state-appropriate actions.

BrowserSkill instance `245ea108` was healthy. A session was started, but the
only ordinary authenticated Odoo tab (`Acme Corporation`, localhost:8069) was
already borrowed by another team session. A second existing PDF tab remained
pending confirmation and was cancelled without navigation. No source screen
could be opened or captured, so desktop/mobile visual evidence is unavailable
for this feature.
