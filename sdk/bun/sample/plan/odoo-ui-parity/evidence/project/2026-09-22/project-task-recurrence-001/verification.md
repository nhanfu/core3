# Verification boundary

Odoo desktop reference capture: `odoo-task-form-desktop.png`.

Not verified in Core3 browser: authenticated desktop/mobile recurrence route,
mobile Odoo recurrence capture, and end-to-end browser mutation/reload. Shared
startup is blocked by Employees discovery failing on
`services/employees/api/employee-detail.yaml` (`actions[7].fields must be a
non-empty array`); Project-only task detail also reports the pre-existing
unregistered `yaml.service.timesheets` dependency. These are blockers, not
visual-parity claims.
