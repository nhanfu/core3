# Verification decision

The YAML page/API/action contract, `project.write` permission boundary,
recursive durable clone, source-version increment, active/company scope, stale
guard, and file-backed reopen all pass the focused and Project regression
suites.

BrowserSkill instance `245ea108` was connected. Session `urpe` listed user tab
`1770663154`, whose existing URL was an Odoo localhost PDF tab. The required
borrow request waited for human confirmation and ended with:
`error: timed out waiting for human confirmation` and
`reason: Timed out waiting for tab borrow confirmation`. Session `urpe` was
stopped cleanly. No credentials, cookies, tokens, Playwright, independent
browser, or alternate tab was used.

This is conditional acceptance: no authenticated Odoo/Core3 desktop or mobile
visual-parity claim is made. Full mail/chatter/portal side effects and
recurrence-rule copy remain open.
