# Verification decision

The YAML page/API/action contract, manager permission boundary, durable project
copy, milestone/task-tree mapping, dependency remapping, fixed-date persistence,
and active/company/stale guards pass the focused and Project regression suites.

BrowserSkill instance `245ea108` was connected. Session `hytm` listed existing
authenticated Odoo user tabs at `localhost:8069`; the single borrow attempt did
not return a successful confirmation before the window closed, and the
follow-up listing left the tab user-scoped. Session `hytm` was stopped cleanly.
No credentials, cookies, tokens, Playwright, independent browser, or alternate
login was used.

This is conditional acceptance: no authenticated Odoo/Core3 desktop or mobile
visual-parity claim is made. Chatter/follower, embedded-action, attachment,
portal, and full recurrence/history side effects remain open.
