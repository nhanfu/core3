# Project Task Recurrence — evidence

Feature: `PROJECT-TASK-RECURRENCE-001`.

The committed screenshot is an authenticated live Odoo 19 task form showing
the recurrence controls and `Recurring Tasks` stat button for Customer review.
The implementation is covered by the Project YAML/API contracts, migration,
guarded mutations, completion workflow, and restart integration tests.

Core3 browser evidence is intentionally absent. Shared discovery is blocked by
the unrelated Employees `employee-detail.yaml` validation error, and the
Project-only task detail prefetch requires the unregistered Timesheets service.
No mobile or visual-parity sign-off is claimed.
