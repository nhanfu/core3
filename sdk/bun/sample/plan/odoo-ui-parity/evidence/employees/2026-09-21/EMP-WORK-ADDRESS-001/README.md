# EMP-WORK-ADDRESS-001 evidence

This slice covers Odoo's employee-specific `hr.version.address_id` Work Address
assignment in the Work tab. Core3 persists a company-scoped address relation on
the employee and active Payroll version, exposes a guarded assignment action,
and keeps page YAML separate from API/action YAML.

Authenticated Odoo desktop/mobile captures are included. Core3 runtime
availability and any fixture/company limitation are recorded in
`verification.md`; no aggregate Employees sign-off is claimed.
