# EMP-WORK-LOCATION-ASSIGNMENT-001 evidence

This slice covers Odoo's employee-specific `hr.version.work_location_id`
assignment in the Work tab. Core3 persists the selected Work Location on the
employee and active Payroll version, exposes a guarded employee action, and
keeps page YAML separate from API/action YAML.

Authenticated Odoo captures are included for desktop and mobile. Core3 runtime
availability and any fixture/company limitation are recorded in
`verification.md`; no aggregate Employees sign-off is claimed.
