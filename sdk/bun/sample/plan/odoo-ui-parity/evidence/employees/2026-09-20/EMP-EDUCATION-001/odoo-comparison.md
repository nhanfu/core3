# Authenticated Odoo comparison

The authenticated Odoo detail is `/odoo/employees/6` in `core3_reference`.
The existing authenticated Family capture visibly includes the exact Education
group and labels at both required viewports:

- Desktop: `../EMP-FAMILY-INFO-001/odoo-desktop.png` (`1440x1000`)
- Mobile: `../EMP-FAMILY-INFO-001/odoo-mobile.png` (`390x844`)
- Probe: `../EMP-FAMILY-INFO-001/odoo-browser.json`

The probe contains `EDUCATION`, `Certificate Level`, and `Field of Study` at
both sizes with matching viewport widths. Known unrelated Odoo shell noise is
documented there; no fresh image is invented.
