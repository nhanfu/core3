# EMP-BIRTH-IDENTITY-001 evidence

Authenticated Core3 and Odoo Personal-tab evidence for employee place of
birth, country of birth, and gender.

- Core3: `admin@tms.local`, `127.0.0.1:3310`, desktop `1440x900` and mobile
  `390x844`; all three labels render with empty page/HTTP error arrays. The
  fixture values are withheld by the current-company guard: session `Core3
  Vietnam Branch`, fixture `Core3 Vietnam`.
- Odoo: `codex@core3.local`, database `core3_reference`, Abigail Peterson at
  `/odoo/employees/6`, Personal tab, desktop `1440x1000` and mobile `390x844`.
  Place of Birth and Gender render. Exact Country of Birth is absent in this
  reference state; Nationality (Country) renders elsewhere. Seven known
  app-icon 404s are shell noise and page errors are empty.

Conditional comparison evidence only; no aggregate Employees sign-off.
