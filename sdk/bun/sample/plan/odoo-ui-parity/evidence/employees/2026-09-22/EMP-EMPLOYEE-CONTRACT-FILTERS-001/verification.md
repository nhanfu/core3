# Verification and blockers

BrowserSkill status showed Chrome instance `245ea108`. Tab `1770662590` was
listed but was borrowed by session `nhqc`; the task-owned session could not
observe or borrow it. No Odoo screenshot was captured.

Core3 runtime probes to `http://localhost:{3000,3001,3002,3003}/api/auth/me`
all returned connection refused. No authenticated Core3 screenshot was
captured.

Required recapture: Odoo/Core3 Employees list with each contract filter at
1440x900 and 390x844, plus a non-manager check that both manager-only filters
are hidden. No visual parity claim is made. The exact manager-only filter UI
boundary is also an open shared YAML/ListView contract gap.
