# EMP-EMPLOYEE-PROPERTIES-GROUP-001

Bounded source-backed Employees parity slice for Odoo's Properties group-by.
The YAML page/API contracts remain separate and join through `page.id:
employees`. The existing durable employee Properties JSON is now available to
the shared Employees group-by and pivot metadata.

Status: implementation complete; Odoo authenticated browser observation is
recorded in `browser.md`.
