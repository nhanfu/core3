# BrowserSkill check

- BrowserSkill daemon: connected instance `245ea108`.
- Odoo target: `http://localhost:8069`, database `core3_reference`.
- A session was started and the user-scoped tab list was inspected.
- The existing authenticated Odoo tab was requested through `bsk tab borrow` with confirmation enabled; confirmation did not complete before the session work ended.
- No independent credentials, Playwright session, or confirmation bypass was used.
- No tab was borrowed, navigated, or modified, so there is no authenticated desktop/mobile capture and no visual-parity claim.
- The local source/ledger record that `website_forum` is not installed in `core3_reference` remains an additional Odoo reference limitation.
