# Fleet vehicle tag assignment

Feature ID: `fleet-vehicle-tags-20260922`

This slice adds the Odoo `fleet.vehicle.tag_ids` many-to-many workflow to the
existing `vehicle-detail` page/API pair. It is distinct from driver mail,
contract renewal activities, and vehicle attachments.

Artifacts: `odoo-analysis.md`, `source-comparison.md`, `gap-matrix.md`,
`functionality-checklist.md`, `test-results.md`, and `verification.md`.

No passwords, cookies, tokens, or Core3 visual captures are stored here.
