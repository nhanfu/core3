# Odoo analysis

- Revision: `659759969d535d286b656c96b675e4612b925ddd` (`65975996`)
- Model: `/home/nhanjs/projects/odoo/addons/fleet/models/fleet_vehicle.py`
- Form: `/home/nhanjs/projects/odoo/addons/fleet/views/fleet_vehicle_views.xml`
- `fleet.vehicle` inherits `mail.thread` and `mail.activity.mixin`.
- `fleet_vehicle_view_form` ends with `<chatter/>`.
- The existing `fleet_vehicle_action` remains Fleet's `kanban,list,form,pivot,activity`
  action; this feature adds no menu.

Live visual inspection was blocked by the required BrowserSkill borrow
confirmation and is not claimed.
