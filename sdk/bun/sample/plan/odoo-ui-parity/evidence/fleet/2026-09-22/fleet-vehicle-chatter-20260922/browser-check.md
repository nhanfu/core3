# BrowserSkill check

- Instance: `245ea108`
- Session: `fqey` (stopped after the timeout)
- User tab: `1770662590`
- Result: borrow timed out awaiting required confirmation
- Cleanup: emulation cleared and session stopped; user tab remained untouched
- Captures: `browser-agent-window-blocker-desktop.png` and
  `browser-agent-window-blocker-mobile.png`

Captures are blocker evidence only, not Odoo/Core3 visual evidence.
