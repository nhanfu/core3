# Gap matrix

| Gap | Evidence | Disposition |
| --- | --- | --- |
| Core3 shared `server_form` click does not dispatch for `Send PO` | Authenticated desktop/mobile Core3 replay; no `/api/mutate`, no modal, no application exception | Open shared-runtime blocker; no Core3 modal parity claim |
| Binary PDF generation and external mail delivery | Existing Purchase print/email boundaries | Follow-up outside this bounded contract |
| Vendor-facing acknowledgement URL | Core3 body uses the authenticated detail route as a deterministic local link | Follow-up when portal/mail URL contract exists |
| Order state model has `Received` in addition to Odoo's `purchase` | Core3 `Received` is accepted alongside `Confirmed` | Deliberate state mapping for this slice |
