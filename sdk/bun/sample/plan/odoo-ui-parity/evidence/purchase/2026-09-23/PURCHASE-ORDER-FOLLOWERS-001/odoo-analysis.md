# Odoo analysis

The source action is the purchase-order `mail.followers` edit action exposed
by the Purchase views. The bounded Core3 contract preserves add/remove
semantics and durable subscription state while keeping YAML page and API
fragments separate and joined by `purchase-detail`.
