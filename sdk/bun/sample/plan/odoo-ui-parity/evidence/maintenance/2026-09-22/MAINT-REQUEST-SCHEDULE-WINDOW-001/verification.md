# Verification boundary

The source-backed schedule-window behavior is covered by the focused and full
Maintenance integration suites, migration replay, deterministic one-hour
default, explicit duration persistence, stale-row handling, invalid timestamp,
and end-before-start guards. Audit and frontend/CSS checks passed.

Authenticated browser comparison is intentionally open because the shared tab
borrow confirmation did not complete. This evidence is not a Maintenance
module sign-off.
