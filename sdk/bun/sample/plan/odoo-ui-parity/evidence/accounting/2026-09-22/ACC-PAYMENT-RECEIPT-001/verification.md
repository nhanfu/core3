# Verification boundary

Core3 verification is contract- and persistence-based for this slice. The
authenticated Odoo visual pass was blocked before tab ownership: BrowserSkill
instance `245ea108` was connected, but the existing authenticated Odoo tab's
borrow confirmation did not complete before the CLI timeout. No independent
browser or login was used. Therefore this evidence does not claim desktop or
mobile visual parity.

Required follow-up: borrow the existing signed-in tab, inspect the payment form
action at desktop `1440x900` and mobile `390x844`, capture the composer and
queued/send states, and return the tab.
