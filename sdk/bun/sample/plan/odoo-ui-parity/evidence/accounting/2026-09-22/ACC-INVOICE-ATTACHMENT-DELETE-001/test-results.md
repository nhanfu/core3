# Test results

Focused command:

```text
bun test test/accounting_invoice_attachments.integration.test.ts
4 pass, 0 fail, 29 expect() calls
```

The suite covers page/API/storage binding, upload/download regression,
atomic removal and chatter persistence, active-source invisibility, actor and
stale guards, missing rows, and no partial writes.
