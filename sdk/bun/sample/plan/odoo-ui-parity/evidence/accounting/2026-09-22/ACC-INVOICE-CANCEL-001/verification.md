# Verification

Core3 contract and persistence verification passed as recorded in
`test-results.md`.

Authenticated Odoo/Core3 desktop and mobile verification is blocked. The
shared BrowserSkill instance was connected, but the authenticated user tab
borrow request timed out waiting for human confirmation. No screenshot was
captured and no Odoo/Core3 visual-parity claim is made. The exact command and
result are in `browser-check.md`.
