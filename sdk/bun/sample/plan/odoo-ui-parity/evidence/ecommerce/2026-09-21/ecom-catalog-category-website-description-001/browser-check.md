# Browser and reference checks

Date: 2026-09-21

## Core3

No authenticated desktop/mobile screenshot was captured: this session has no
persistent `js_repl` browser runtime, and Core3 ports `3000`, `4312`, and
`4313` returned connection failure (`000`) during bounded probes. No rendered
UI or responsive sign-off is claimed.

## Odoo

The supplied reference endpoints on ports `8069` and `8073` returned exact
HTTP `404` for `/shop`. Authenticated desktop/mobile Website Sale comparison
is therefore blocked by the unavailable route/runtime.
