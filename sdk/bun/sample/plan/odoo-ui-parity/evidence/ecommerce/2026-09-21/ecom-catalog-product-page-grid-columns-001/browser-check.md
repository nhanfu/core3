# Browser and Odoo checks

- Core3 authenticated desktop/mobile capture: blocked because the required
  local runtime/browser session is unavailable; no screenshots or rendered UI
  sign-off are claimed.
- Odoo authenticated comparison: the supplied Website Sale `/shop` route is
  an exact HTTP 404 on ports 8069 and 8073, so the paired product-page image
  grid comparison cannot be completed.
- Source comparison is exact against the supplied Odoo model, builder plugin,
  builder view, and product template; this does not substitute for browser
  evidence.
