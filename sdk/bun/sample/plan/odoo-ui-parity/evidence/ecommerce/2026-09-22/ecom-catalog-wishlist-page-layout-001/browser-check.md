# Browser check

The requested bsk browser instance `245ea108` was used with the shared
authenticated Odoo QA session. The bsk session was stopped after the desktop
and iPhone-14 captures were written. Credentials, cookies, and tokens were
not read or recorded.

The exact authenticated route result was HTTP 404 / Odoo Error 404 at both
viewports. Core3 was not claimed as rendered because its local target ports
were unavailable.
