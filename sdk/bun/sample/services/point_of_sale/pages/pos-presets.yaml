title: POS Presets
page: { id: pos-presets, route: /point-of-sale/presets, breadcrumb: [Sales, Point of Sale, Configuration, Presets], auth: { require: [pos.read] } }
components:
  - type: ComingSoon
    eyebrow: Configuration
    title: Order presets
    description: POS order presets and default preparation behavior are tracked here.
