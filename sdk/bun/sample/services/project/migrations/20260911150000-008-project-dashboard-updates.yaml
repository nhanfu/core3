version: 0.0.8
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS project_updates (
        id VARCHAR PRIMARY KEY,
        project_id VARCHAR NOT NULL,
        name VARCHAR NOT NULL,
        status VARCHAR NOT NULL DEFAULT 'on_track',
        progress INTEGER NOT NULL DEFAULT 0,
        user_name VARCHAR NOT NULL DEFAULT 'Project Team',
        date DATE NOT NULL DEFAULT DATE '2026-01-15',
        description VARCHAR NOT NULL DEFAULT '',
        row_version BIGINT NOT NULL DEFAULT 1,
        created_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00',
        updated_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00'
      );
      INSERT OR IGNORE INTO project_updates(id, project_id, name, status, progress, user_name, date, description, row_version, created_at, updated_at)
      VALUES
        ('project-update-001', 'project-demo-001', 'Construction', 'on_track', 30, 'Mitchell Admin', DATE '2026-01-15', 'Construction is progressing according to the approved plan.', 1, TIMESTAMP '2026-01-15 00:00:00', TIMESTAMP '2026-01-15 00:00:00'),
        ('project-update-002', 'project-demo-001', 'Design Approval', 'on_track', 65, 'Mitchell Admin', DATE '2026-01-12', 'The design package is ready for the next review.', 1, TIMESTAMP '2026-01-15 00:00:00', TIMESTAMP '2026-01-15 00:00:00'),
        ('project-update-003', 'project-demo-002', 'Customer Brief', 'at_risk', 40, 'Project Team', DATE '2026-01-14', 'The customer brief needs confirmation before execution starts.', 1, TIMESTAMP '2026-01-15 00:00:00', TIMESTAMP '2026-01-15 00:00:00'),
        ('project-update-004', 'project-demo-002', 'Wireframes', 'done', 100, 'Project Team', DATE '2026-01-10', 'The replacement wireframes were approved.', 1, TIMESTAMP '2026-01-15 00:00:00', TIMESTAMP '2026-01-15 00:00:00')
      ;
    down: |
      DROP TABLE IF EXISTS project_updates;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
