version: 0.0.11
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS project_settings (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        company_name VARCHAR NOT NULL DEFAULT 'My Company',
        group_project_stages BOOLEAN NOT NULL DEFAULT FALSE,
        module_hr_timesheet BOOLEAN NOT NULL DEFAULT FALSE,
        updated_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00'
      );
      INSERT INTO project_settings (id, row_version, company_name, group_project_stages, module_hr_timesheet)
      VALUES ('project-settings-demo', 1, 'My Company', TRUE, FALSE)
      ON CONFLICT (id) DO NOTHING;
    down: |
      DROP TABLE IF EXISTS project_settings;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
