id: project
kind: domain-service
database: duckdb
migrations: migrations
permissions: permissions.yaml
storage: storage.yaml
menu:
  project:
    path: /projects
    label: Project
    icon: project
    permission: project.read
  groups:
    - id: delivery
      label: Delivery
      items:
        - { path: /projects, label: Projects, icon: project, permission: project.read }
        - { path: /project-by-stage, label: Projects, icon: workflow, permission: project.read }
        - { path: /tasks, label: My Tasks, icon: checklist, permission: project.read }
    - id: reporting
      label: Reporting
      items:
        - { path: /tasks-analysis, label: Tasks Analysis, icon: chart, permission: project.read }
        - { path: /customer-ratings, label: Customer Ratings, icon: star, permission: project.read }
    - id: configuration
      label: Configuration
      items:
        - { path: /project-stages, label: Project Stages, icon: workflow, permission: project.manage }
        - { path: /task-tags, label: Tags, icon: tag, permission: project.manage }
        - { path: /project-roles, label: Project Roles, icon: users, permission: project.manage }
        - { path: /project-activity-types, label: Activity Types, icon: activity, permission: project.manage }
