version: 0.0.21
description: Index durable Work Orders for the Work Center scoped action
kind: schema
type:
  postgres:
    up: |
      CREATE INDEX IF NOT EXISTS idx_mrp_workorders_workcenter_state_plan
        ON mrp_workorders(workcenter, state, planned_date, sequence);
    down: |
      DROP INDEX IF EXISTS idx_mrp_workorders_workcenter_state_plan;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
