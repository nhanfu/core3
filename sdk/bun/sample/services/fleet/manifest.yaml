id: fleet
kind: domain-service
database: duckdb
migrations: migrations
permissions: permissions.yaml
storage: storage.yaml
menu:
  fleet:
    path: /vehicles
    label: Fleet
    icon: car
    permission: fleet.read
  groups:
    - id: vehicles
      label: Fleet
      items:
        - { path: /vehicles, label: Fleet, icon: car, permission: fleet.read }
        - { path: /fleet/odometers, label: Odometers, icon: chart, permission: fleet.read }
        - { path: /fleet/contracts, label: Contracts, icon: file, permission: fleet.read }
    - id: reporting
      label: Reporting
      items:
        - { path: /fleet-analysis, label: Fleet Analysis, icon: chart, permission: fleet.read }
    - id: configuration
      label: Configuration
      items:
        - { path: /fleet/config/manufacturers, label: Manufacturers, icon: car, permission: fleet.manage }
        - { path: /fleet/config/models, label: Models, icon: car, permission: fleet.manage }
