id: fleet
kind: domain-service
database: duckdb
migrations: migrations
permissions: permissions.yaml
storage: storage.yaml
menu:
  fleet:
    path: /vehicles
    label: Fleet
    icon: car
    permission: fleet.read
  groups:
    - id: vehicles
      label: Vehicles
      items:
        - { path: /vehicles, label: Vehicles, icon: car, permission: fleet.read }
        - { path: /fleet-analysis, label: Fleet Analysis, icon: chart, permission: fleet.read }
