version: 0.0.19
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS fleet_settings (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        company_name VARCHAR NOT NULL DEFAULT 'My Company (San Francisco)',
        delay_alert_contract INTEGER NOT NULL DEFAULT 30,
        updated_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00'
      );
    down: |
      DROP TABLE IF EXISTS fleet_settings;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
