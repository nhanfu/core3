version: 0.0.23
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS fleet_vehicle_tags (
        id VARCHAR PRIMARY KEY,
        name VARCHAR NOT NULL,
        color INTEGER NOT NULL DEFAULT 0,
        row_version BIGINT NOT NULL DEFAULT 1,
        created_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00',
        updated_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00'
      );
      CREATE UNIQUE INDEX IF NOT EXISTS fleet_vehicle_tags_name_idx ON fleet_vehicle_tags(LOWER(name));
    down: |
      DROP INDEX IF EXISTS fleet_vehicle_tags_name_idx;
      DROP TABLE IF EXISTS fleet_vehicle_tags;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
