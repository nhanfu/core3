version: 0.0.27
kind: schema
type:
  postgres:
    up: |
      ALTER TABLE fleet_service_types ADD COLUMN IF NOT EXISTS row_version BIGINT;
      UPDATE fleet_service_types SET row_version = 1 WHERE row_version IS NULL;
      CREATE INDEX IF NOT EXISTS fleet_service_types_category_name_idx ON fleet_service_types(category, name, id);
    down: |
      DROP INDEX IF EXISTS fleet_service_types_category_name_idx;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
