version: 0.0.25
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS fleet_activity_types (
        id VARCHAR PRIMARY KEY, row_version BIGINT NOT NULL DEFAULT 1,
        name VARCHAR NOT NULL, sequence INTEGER NOT NULL DEFAULT 10,
        active BOOLEAN NOT NULL DEFAULT TRUE, summary VARCHAR,
        category VARCHAR NOT NULL DEFAULT 'default', default_user_name VARCHAR,
        res_model VARCHAR, delay_count INTEGER NOT NULL DEFAULT 0,
        delay_unit VARCHAR NOT NULL DEFAULT 'days',
        delay_from VARCHAR NOT NULL DEFAULT 'previous_activity',
        chaining_type VARCHAR NOT NULL DEFAULT 'suggest',
        triggered_next_name VARCHAR, suggested_next_summary VARCHAR,
        icon VARCHAR, default_note VARCHAR,
        created_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00',
        updated_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00'
      );
    down: DROP TABLE IF EXISTS fleet_activity_types;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
