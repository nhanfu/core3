page: { id: model-detail }
datasources:
  - id: fleet_model_detail_manufacturers
    permission: fleet.read
    query: "SELECT id AS value, name AS label FROM fleet_vehicle_model_brands WHERE active = TRUE ORDER BY name, id"
  - id: fleet_model_detail_categories
    permission: fleet.read
    query: "SELECT id AS value, name AS label FROM fleet_vehicle_model_categories WHERE active = TRUE ORDER BY sequence, name, id"
  - id: fleet_model_vendor_options
    permission: fleet.manage
    query: "SELECT v.id AS value, v.name || CASE WHEN v.email = '' THEN '' ELSE ' · ' || v.email END AS label FROM fleet_vendor_contacts v JOIN fleet_vehicle_models model ON model.id = :id AND model.active = TRUE WHERE v.active = TRUE AND NOT EXISTS (SELECT 1 FROM fleet_vehicle_model_vendors r WHERE r.model_id = :id AND r.vendor_id = v.id) ORDER BY v.name, v.id"
  - id: fleet_model_vendor_assignments
    permission: fleet.read
    error_states: { transport_error: { status: 503, code: FLEET_MODEL_VENDORS_UNAVAILABLE, message: Model vendors are temporarily unavailable. } }
    query: "SELECT r.model_id, v.id, v.id AS vendor_id, v.name, v.email, v.phone, v.active, 1 AS row_version FROM fleet_vehicle_model_vendors r JOIN fleet_vendor_contacts v ON v.id = r.vendor_id JOIN fleet_vehicle_models model ON model.id = r.model_id WHERE r.model_id = :id AND CASE WHEN COALESCE(:fixture_state, '') IN ('empty', 'not_found') THEN FALSE ELSE TRUE END AND model.id = :id ORDER BY v.name, v.id"
  - id: fleet_model_detail
    single: true
    permission: fleet.read
    error_states: { transport_error: { status: 503, code: FLEET_MODEL_DETAIL_UNAVAILABLE, message: Model details are temporarily unavailable. } }
    query: "SELECT model.id, model.row_version, model.brand_id, brand.name AS brand_name, model.category_id, category.name AS category_name, model.name, model.vehicle_count, model.vehicle_type, CASE model.vehicle_type WHEN 'bike' THEN 'Bike' ELSE 'Car' END AS vehicle_type_label, model.model_year, model.seats, model.doors, model.color, model.trailer_hook, model.electric_assistance, model.default_fuel_type, model.transmission, model.drive_type, model.power, model.power_unit, model.vehicle_range, model.range_unit, model.default_co2, model.co2_emission_unit, model.co2_standard, model.active, CASE WHEN model.active THEN 'Active' ELSE 'Archived' END AS active_label, COALESCE((SELECT string_agg(v.name, ', ' ORDER BY v.name, v.id) FROM fleet_vehicle_model_vendors r JOIN fleet_vendor_contacts v ON v.id = r.vendor_id WHERE r.model_id = model.id AND v.active = TRUE), '') AS vendor_summary FROM fleet_vehicle_models AS model JOIN fleet_vehicle_model_brands AS brand ON brand.id = model.brand_id LEFT JOIN fleet_vehicle_model_categories AS category ON category.id = model.category_id WHERE CASE WHEN COALESCE(:fixture_state, '') IN ('empty', 'not_found') THEN 0 ELSE 1 END = 1 AND model.id = :id"
actions:
  - { id: back_to_fleet_models, type: navigate, permission: fleet.read, navigate_to: /fleet/config/models }
  - id: edit_fleet_model
    type: server_form
    permission: fleet.manage
    title: Edit Model
    action: fleet.models.update
    handler: yaml_mutation
    operation: update
    prefill: state.fleet_model_detail
    refresh: [fleet_model_detail, fleet_models, fleet_model_manufacturers]
    mutation:
      operation: update
      table: fleet_vehicle_models
      fields: [brand_id, category_id, name, vehicle_type, model_year, seats, doors, color, trailer_hook, electric_assistance, default_fuel_type, transmission, drive_type, power, power_unit, vehicle_range, range_unit, default_co2, co2_emission_unit, co2_standard]
      required: [brand_id, name, vehicle_type]
      concurrency: { required: true }
      guards:
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :id", status: 404, code: FLEET_MODEL_NOT_FOUND, message: Model was not found. }
        - { query: "SELECT id FROM fleet_vehicle_model_brands WHERE id = :brand_id AND active = TRUE", status: 404, code: FLEET_MODEL_MANUFACTURER_NOT_FOUND, message: Manufacturer was not found or is archived. }
        - { query: "SELECT 1 AS ok WHERE :category_id IS NULL OR EXISTS (SELECT 1 FROM fleet_vehicle_model_categories WHERE id = :category_id AND active = TRUE)", status: 404, code: FLEET_MODEL_CATEGORY_NOT_FOUND, message: Category was not found or is archived. }
        - { query: "SELECT 1 AS ok WHERE LENGTH(TRIM(CAST(:name AS VARCHAR))) BETWEEN 1 AND 80", status: 422, code: FLEET_MODEL_NAME_REQUIRED, message: Model name is required. }
        - { query: "SELECT 1 AS ok WHERE :vehicle_type IN ('car', 'bike')", status: 422, code: FLEET_MODEL_VEHICLE_TYPE_INVALID, message: Vehicle Type must be Car or Bike. }
        - { query: "SELECT 1 AS ok WHERE NOT EXISTS (SELECT 1 FROM fleet_vehicle_models WHERE brand_id = :brand_id AND LOWER(name) = LOWER(TRIM(CAST(:name AS VARCHAR))) AND id <> :id)", status: 409, code: FLEET_MODEL_EXISTS, message: A model with this name already exists for this manufacturer. }
        - { query: "SELECT 1 AS ok WHERE CAST(COALESCE(:seats, 0) AS INTEGER) >= 0 AND CAST(COALESCE(:doors, 0) AS INTEGER) >= 0 AND CAST(COALESCE(:power, 0) AS DECIMAL(12,2)) >= 0 AND CAST(COALESCE(:vehicle_range, 0) AS INTEGER) >= 0 AND CAST(COALESCE(:default_co2, 0) AS DECIMAL(12,2)) >= 0", status: 422, code: FLEET_MODEL_METRICS_INVALID, message: Model measurements cannot be negative. }
      before_steps: [{ query: "SELECT brand_id AS old_brand_id, active AS old_active FROM fleet_vehicle_models WHERE id = :id", assign: true }]
      steps: [{ query: "UPDATE fleet_vehicle_model_brands SET model_count = model_count - 1 WHERE id = :old_brand_id AND :old_brand_id <> :brand_id AND :old_active = TRUE" }, { query: "UPDATE fleet_vehicle_model_brands SET model_count = model_count + 1 WHERE id = :brand_id AND :old_brand_id <> :brand_id AND active = TRUE" }]
      result: { query: "SELECT * FROM fleet_vehicle_models WHERE id = :id" }
    fields: [{ field: name, label: Model name, type: text, required: true }, { field: brand_id, label: Manufacturer, type: select, options_source: fleet_model_detail_manufacturers, required: true }, { field: category_id, label: Category, type: select, options_source: fleet_model_detail_categories }, { field: vehicle_type, label: Vehicle Type, type: select, options: [{ value: car, label: Car }, { value: bike, label: Bike }], required: true }, { field: model_year, label: Model Year, type: text }, { field: seats, label: Seating Capacity, type: number }, { field: doors, label: Number of Doors, type: number }, { field: color, label: Color, type: text }, { field: trailer_hook, label: Trailer Hitch, type: checkbox }, { field: electric_assistance, label: Electric Assistance, type: checkbox }, { field: default_fuel_type, label: Fuel Type, type: select, options: [{ value: diesel, label: Diesel }, { value: gasoline, label: Gasoline }, { value: full_hybrid, label: Full Hybrid }, { value: electric, label: Electric }] }, { field: transmission, label: Transmission, type: select, options: [{ value: manual, label: Manual }, { value: automatic, label: Automatic }] }, { field: drive_type, label: Drive Type, type: select, options: [{ value: fwd, label: Front-Wheel Drive (FWD) }, { value: awd, label: All-Wheel Drive (AWD) }, { value: rwd, label: Rear-Wheel Drive (RWD) }, { value: 4wd, label: Four-Wheel Drive (4WD) }] }, { field: power, label: Power, type: number }, { field: power_unit, label: Power Unit, type: select, options: [{ value: power, label: kW }, { value: horsepower, label: Horsepower (hp) }] }, { field: vehicle_range, label: Range, type: number }, { field: range_unit, label: Range Unit, type: select, options: [{ value: km, label: km }, { value: mi, label: mi }] }, { field: default_co2, label: CO₂ Emissions, type: number }, { field: co2_emission_unit, label: Emission Unit, type: text }, { field: co2_standard, label: Emission Standard, type: text }]
  - id: add_fleet_model_vendor
    type: server_form
    permission: fleet.manage
    title: Add Vendor
    description: Assign an Odoo vendor contact to this vehicle model.
    action: fleet.models.vendors.add
    handler: line_item
    operation: create
    domain: fleet_model_detail
    params: { id: '{state.fleet_model_detail.id}', parent_expected_row_version: '{state.fleet_model_detail.row_version}' }
    refresh: [fleet_model_detail, fleet_model_vendor_assignments, fleet_model_vendor_options]
    fields:
      - { field: vendor_id, label: Vendor, type: select, options_source: fleet_model_vendor_options, required: true }
    mutation:
      guards:
        - { query: "SELECT 1 AS ok WHERE NULLIF(TRIM(CAST(:current_user_id AS VARCHAR)), '') IS NOT NULL", status: 403, code: FLEET_MODEL_VENDOR_ACTOR_REQUIRED, message: An authenticated Fleet actor is required. }
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :id AND active = TRUE", status: 404, code: FLEET_MODEL_VENDOR_MODEL_NOT_FOUND, message: Active model was not found. }
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :id AND active = TRUE AND row_version = :parent_expected_row_version", status: 409, code: STALE_RECORD, message: The model changed before the vendor was added. Reload and try again. }
        - { query: "SELECT id FROM fleet_vendor_contacts WHERE id = :vendor_id AND active = TRUE", status: 422, code: FLEET_MODEL_VENDOR_INVALID, message: Select an active vendor. }
        - { query: "SELECT 1 AS ok WHERE NOT EXISTS (SELECT 1 FROM fleet_vehicle_model_vendors WHERE model_id = :id AND vendor_id = :vendor_id)", status: 409, code: FLEET_MODEL_VENDOR_EXISTS, message: This vendor is already assigned to the model. }
      steps:
        - { query: "INSERT INTO fleet_vehicle_model_vendors(model_id, vendor_id) VALUES(:id, :vendor_id)", expect_changed: true, status: 409, code: FLEET_MODEL_VENDOR_CREATE_FAILED, message: The vendor could not be assigned to the model. }
        - { query: "UPDATE fleet_vehicle_models SET row_version = row_version + 1, updated_at = TIMESTAMP '2026-01-15 00:00:00' WHERE id = :id AND active = TRUE AND row_version = :parent_expected_row_version", expect_changed: true, status: 409, code: STALE_RECORD, message: The model changed while assigning the vendor. Reload and try again. }
      result: { query: "SELECT r.model_id, v.id, v.id AS vendor_id, v.name, v.email, v.phone, v.active, 1 AS row_version FROM fleet_vehicle_model_vendors r JOIN fleet_vendor_contacts v ON v.id = r.vendor_id WHERE r.model_id = :id AND r.vendor_id = :vendor_id" }
  - id: remove_fleet_model_vendor
    type: server
    permission: fleet.manage
    action: fleet.models.vendors.remove
    handler: line_item
    operation: delete
    domain: fleet_model_vendor_assignment
    params: { id: '{state.fleet_model_detail.id}', vendor_id: '{row.vendor_id}', parent_expected_row_version: '{state.fleet_model_detail.row_version}' }
    refresh: [fleet_model_detail, fleet_model_vendor_assignments, fleet_model_vendor_options]
    mutation:
      guards:
        - { query: "SELECT 1 AS ok WHERE NULLIF(TRIM(CAST(:current_user_id AS VARCHAR)), '') IS NOT NULL", status: 403, code: FLEET_MODEL_VENDOR_ACTOR_REQUIRED, message: An authenticated Fleet actor is required. }
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :id AND active = TRUE", status: 404, code: FLEET_MODEL_VENDOR_MODEL_NOT_FOUND, message: Active model was not found. }
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :id AND active = TRUE AND row_version = :parent_expected_row_version", status: 409, code: STALE_RECORD, message: The model changed before the vendor was removed. Reload and try again. }
        - { query: "SELECT vendor_id FROM fleet_vehicle_model_vendors WHERE model_id = :id AND vendor_id = :vendor_id", status: 409, code: FLEET_MODEL_VENDOR_STALE, message: This model vendor changed or was removed; reload and try again. }
      steps:
        - { query: "DELETE FROM fleet_vehicle_model_vendors WHERE model_id = :id AND vendor_id = :vendor_id", expect_changed: true, status: 409, code: FLEET_MODEL_VENDOR_STALE, message: This model vendor changed or was removed; reload and try again. }
        - { query: "UPDATE fleet_vehicle_models SET row_version = row_version + 1, updated_at = TIMESTAMP '2026-01-15 00:00:00' WHERE id = :id AND active = TRUE AND row_version = :parent_expected_row_version", expect_changed: true, status: 409, code: STALE_RECORD, message: The model changed while removing the vendor. Reload and try again. }
      result: { query: "SELECT :id AS model_id, :vendor_id AS vendor_id" }
  - id: archive_fleet_model
    type: server
    permission: fleet.manage
    action: fleet.models.archive
    handler: yaml_mutation
    operation: update
    refresh: [fleet_model_detail, fleet_models]
    params: { values: { active: false } }
    mutation: { operation: update, table: fleet_vehicle_models, fields: [active], concurrency: { required: true }, guards: [{ query: "SELECT id FROM fleet_vehicle_models WHERE id = :id AND active = TRUE", status: 404, code: FLEET_MODEL_NOT_FOUND, message: Active model was not found. }], before_steps: [{ query: "SELECT brand_id FROM fleet_vehicle_models WHERE id = :id", assign: true }], steps: [{ query: "UPDATE fleet_vehicle_model_brands SET model_count = model_count - 1 WHERE id = :brand_id AND model_count > 0" }], result: { query: "SELECT * FROM fleet_vehicle_models WHERE id = :id" } }
  - id: unarchive_fleet_model
    type: server
    permission: fleet.manage
    action: fleet.models.unarchive
    handler: yaml_mutation
    operation: update
    refresh: [fleet_model_detail, fleet_models]
    params: { values: { active: true } }
    mutation: { operation: update, table: fleet_vehicle_models, fields: [active], concurrency: { required: true }, guards: [{ query: "SELECT id FROM fleet_vehicle_models WHERE id = :id AND active = FALSE", status: 404, code: FLEET_MODEL_NOT_FOUND, message: Archived model was not found. }], before_steps: [{ query: "SELECT brand_id FROM fleet_vehicle_models WHERE id = :id", assign: true }], steps: [{ query: "UPDATE fleet_vehicle_model_brands SET model_count = model_count + 1 WHERE id = :brand_id" }], result: { query: "SELECT * FROM fleet_vehicle_models WHERE id = :id" } }
  - id: delete_fleet_model
    type: server
    permission: fleet.manage
    action: fleet.models.delete
    handler: yaml_mutation
    operation: delete
    refresh: [fleet_models, fleet_model_manufacturers]
    params: { id: '{row.id}' }
    mutation:
      operation: delete
      table: fleet_vehicle_models
      concurrency: { required: true }
      guards:
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :id", status: 404, code: FLEET_MODEL_NOT_FOUND, message: Model was not found. }
        - { query: "SELECT 1 AS ok FROM fleet_vehicle_models WHERE id = :id AND vehicle_count = 0", status: 409, code: FLEET_MODEL_IN_USE, message: A model assigned to vehicles cannot be deleted; archive it instead. }
      before_steps: [{ query: "SELECT brand_id AS old_brand_id, active AS old_active FROM fleet_vehicle_models WHERE id = :id", assign: true }]
      steps: [{ query: "UPDATE fleet_vehicle_model_brands SET model_count = model_count - 1 WHERE id = :old_brand_id AND model_count > 0 AND :old_active = TRUE" }]
  - id: create_fleet_vehicle_for_model
    type: server_form
    permission: fleet.write
    title: New Vehicle
    description: Create the first vehicle for this model.
    action: fleet.vehicle.model.action_model_vehicle.create
    handler: yaml_mutation
    operation: create
    params: { default_model_id: '{state.fleet_model_detail.id}', expected_model_row_version: '{state.fleet_model_detail.row_version}' }
    prefill: { model_name: '{state.fleet_model_detail.name}' }
    refresh: [fleet_model_detail]
    mutation:
      operation: insert
      table: fleet_vehicles
      fields: [name, license_plate, odometer, fuel_type, acquisition_date, contract_end_date, trailer_hook, company_name]
      required: [name, license_plate]
      defaults: { odometer: 0, trailer_hook: false, company_name: Core3 Demo Company }
      before_steps:
        - { query: "SELECT 'fleet-model-vehicle-' || :default_model_id || '-' || CAST(COUNT(*) + 1 AS VARCHAR) AS id FROM fleet_vehicle_model_rel WHERE model_id = :default_model_id", assign: true }
      guards:
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :default_model_id AND active = TRUE", status: 404, code: FLEET_MODEL_NOT_FOUND, message: Active vehicle model was not found. }
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :default_model_id AND active = TRUE AND vehicle_count = 0", status: 409, code: FLEET_MODEL_VEHICLES_EXIST, message: This model already has a vehicle. Open the Vehicles action instead. }
        - { query: "SELECT 1 AS ok WHERE :expected_model_row_version IS NOT NULL", status: 400, code: FLEET_MODEL_VERSION_REQUIRED, message: Reload the model before creating a vehicle. }
        - { query: "SELECT id FROM fleet_vehicle_models WHERE id = :default_model_id AND row_version = :expected_model_row_version AND active = TRUE AND vehicle_count = 0", status: 409, code: STALE_RECORD, message: The model changed before the vehicle was created. Reload and try again. }
        - { query: "SELECT 1 AS ok WHERE LENGTH(TRIM(CAST(:name AS VARCHAR))) BETWEEN 1 AND 120", status: 422, code: FLEET_VEHICLE_NAME_REQUIRED, message: Vehicle name is required. }
        - { query: "SELECT 1 AS ok WHERE LENGTH(TRIM(CAST(:license_plate AS VARCHAR))) BETWEEN 1 AND 32", status: 422, code: FLEET_VEHICLE_LICENSE_PLATE_REQUIRED, message: License plate is required. }
        - { query: "SELECT 1 AS ok WHERE COALESCE(CAST(:odometer AS DECIMAL(18,3)), 0) >= 0", status: 422, code: FLEET_VEHICLE_ODOMETER_INVALID, message: Odometer cannot be negative. }
        - { query: "SELECT 1 AS ok WHERE (:acquisition_date IS NULL OR (regexp_matches(CAST(:acquisition_date AS VARCHAR), '^[0-9]{4}-[0-9]{2}-[0-9]{2}$') AND CAST(:acquisition_date AS DATE) BETWEEN DATE '1900-01-01' AND DATE '2100-12-31')) AND (:contract_end_date IS NULL OR (regexp_matches(CAST(:contract_end_date AS VARCHAR), '^[0-9]{4}-[0-9]{2}-[0-9]{2}$') AND CAST(:contract_end_date AS DATE) BETWEEN DATE '1900-01-01' AND DATE '2100-12-31')) AND (:acquisition_date IS NULL OR :contract_end_date IS NULL OR CAST(:contract_end_date AS DATE) >= CAST(:acquisition_date AS DATE))", status: 422, code: FLEET_VEHICLE_DATES_INVALID, message: Acquisition and contract dates are invalid. }
        - { query: "SELECT 1 AS ok WHERE NOT EXISTS (SELECT 1 FROM fleet_vehicles WHERE LOWER(TRIM(name)) = LOWER(TRIM(CAST(:name AS VARCHAR))))", status: 409, code: FLEET_VEHICLE_NAME_EXISTS, message: A vehicle with this name already exists. }
        - { query: "SELECT 1 AS ok WHERE NOT EXISTS (SELECT 1 FROM fleet_vehicles WHERE LOWER(TRIM(license_plate)) = LOWER(TRIM(CAST(:license_plate AS VARCHAR))))", status: 409, code: FLEET_VEHICLE_LICENSE_PLATE_EXISTS, message: A vehicle with this license plate already exists. }
        - { query: "SELECT 1 AS ok WHERE COALESCE(:current_company_name, '') = '' OR COALESCE(:company_name, 'Core3 Demo Company') = :current_company_name", status: 403, code: FLEET_VEHICLE_COMPANY_SCOPE_REQUIRED, message: Vehicle must belong to the current company. }
      steps:
        - { query: "INSERT INTO fleet_vehicle_model_rel(vehicle_id, model_id) VALUES(:id, :default_model_id)", expect_changed: true, status: 409, code: FLEET_MODEL_VEHICLE_RELATION_FAILED, message: The vehicle model relation could not be created. }
        - { query: "UPDATE fleet_vehicles SET model = (SELECT name FROM fleet_vehicle_models WHERE id = :default_model_id), manufacturer = (SELECT brand.name FROM fleet_vehicle_models model JOIN fleet_vehicle_model_brands brand ON brand.id = model.brand_id WHERE model.id = :default_model_id), vehicle_type = (SELECT CASE vehicle_type WHEN 'bike' THEN 'Bike' ELSE 'Car' END FROM fleet_vehicle_models WHERE id = :default_model_id), state = 'Available', updated_at = TIMESTAMP '2026-01-15 00:00:00' WHERE id = :id", expect_changed: true, status: 409, code: FLEET_MODEL_VEHICLE_UPDATE_FAILED, message: The vehicle could not be initialized from its model. }
        - { query: "UPDATE fleet_vehicle_models SET vehicle_count = vehicle_count + 1, row_version = row_version + 1, updated_at = TIMESTAMP '2026-01-15 00:00:00' WHERE id = :default_model_id AND active = TRUE AND vehicle_count = 0 AND row_version = :expected_model_row_version", expect_changed: true, status: 409, code: STALE_RECORD, message: The model changed before the vehicle was created. Reload and try again. }
      result: { query: "SELECT v.id, v.row_version, v.name, v.license_plate, v.vehicle_type, v.model, v.manufacturer, v.company_name, r.model_id FROM fleet_vehicles v JOIN fleet_vehicle_model_rel r ON r.vehicle_id = v.id WHERE v.id = :id" }
    fields:
      - { field: model_name, label: Model, type: text, readonly: true }
      - { field: name, label: Vehicle, type: text, required: true, placeholder: Vehicle name }
      - { field: license_plate, label: License plate, type: text, required: true, placeholder: e.g. PAE 326 }
      - { field: odometer, label: Odometer, type: number, default: 0, min: 0 }
      - { field: fuel_type, label: Fuel type, type: text }
      - { field: acquisition_date, label: Registration date, type: text, placeholder: YYYY-MM-DD }
      - { field: contract_end_date, label: Contract end date, type: text, placeholder: YYYY-MM-DD }
      - { field: trailer_hook, label: Trailer Hitch, type: checkbox, default: false }
      - { field: company_name, label: Company, type: hidden, default: Core3 Demo Company }
  - { id: view_fleet_model_vehicles, type: navigate, permission: fleet.read, navigate_to: /vehicles, params: { model_id: '{state.fleet_model_detail.id}', default_model_id: '{state.fleet_model_detail.id}' } }
