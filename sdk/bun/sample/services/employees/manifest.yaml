id: employees
kind: domain-service
database: duckdb
migrations: migrations
permissions: permissions.yaml
storage: storage.yaml
menu:
  employees:
    path: /employees
    label: Employees
    icon: users
    permission: employees.read
  groups:
    - id: people
      label: People
      items:
        - { path: /employees, label: Employees, icon: users, permission: employees.read }
        - { path: /employees/directory, label: Directory, icon: contacts, permission: employees.read }
        - { path: /employees/activities, label: All activities, icon: activity, permission: employees.read }
    - id: reporting
      label: Reporting
      items:
        - { path: /employees/departments, label: Departments, icon: hierarchy, permission: employees.read }
