id: employees
kind: domain-service
database: duckdb
migrations: migrations
permissions: permissions.yaml
storage: storage.yaml
menu:
  employees:
    path: /employees
    label: Employees
    icon: users
    permission: employees.read
  groups:
    - id: people
      label: People
      items:
        - { path: /employees, label: Employees, icon: users, permission: employees.read }
        - { path: /employees/directory, label: Directory, icon: contacts, permission: employees.read }
        - { path: /employees/activities, label: All activities, icon: activity, permission: employees.read }
        - { path: /employees/versions, label: Employee Records, icon: history, permission: employees.read }
    - id: reporting
      label: Reporting
      items:
        - { path: /employees/departments, label: Departments, icon: hierarchy, permission: employees.read }
    - id: learning
      label: Learning
      items:
        - { path: /employees/training-attendances, label: Training Attendances, icon: education, permission: employees.read }
    - id: configuration
      label: Configuration
      items:
        - { path: /employees/settings, label: Settings, icon: settings, permission: employees.settings }
        - { path: /employees/work-locations, label: Work Locations, icon: location, permission: employees.manage }
        - { path: /employees/working-schedules, label: Working Schedules, icon: calendar, permission: employees.read }
        - { path: /employees/departure-reasons, label: Departure Reasons, icon: list, permission: employees.manage }
        - { path: /employees/jobs, label: Job Positions, icon: briefcase, permission: employees.read }
        - { path: /employees/contract-templates, label: Contract Templates, icon: file-text, permission: employees.manage }
        - { path: /employees/employment-types, label: Employment Types, icon: list, permission: employees.read }
        - { path: /employees/activity-plans, label: Onboarding / Offboarding, icon: checklist, permission: employees.manage }
        - { path: /employees/skill-types, label: Skill Types, icon: skills, permission: employees.read }
