id: employees
kind: domain-service
database: duckdb
migrations: migrations
permissions: permissions.yaml
storage: storage.yaml
menu:
  employees:
    path: /employees
    label: Employees
    icon: users
    permission: employees.read
  groups:
    - id: people
      label: People
      items:
        - { path: /employees, label: Employees, icon: users, permission: employees.read }
        - { path: /employee-analysis, label: Employee Analysis, icon: chart, permission: employees.read }
