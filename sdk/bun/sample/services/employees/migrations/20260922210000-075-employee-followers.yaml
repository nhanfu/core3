version: 0.0.75
kind: data
description: Add durable Employee chatter followers and deterministic relation
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS employee_followers (
        employee_id VARCHAR NOT NULL,
        user_id VARCHAR NOT NULL,
        added_by VARCHAR,
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY (employee_id, user_id)
      );
      CREATE INDEX IF NOT EXISTS employee_followers_employee_idx ON employee_followers(employee_id, created_at);
      INSERT INTO employee_followers(employee_id, user_id, added_by, created_at)
      VALUES ('employee-demo-001', 'user-disp', 'user-admin', TIMESTAMP '2026-01-14 10:30:00')
      ON CONFLICT (employee_id, user_id) DO NOTHING;
    down: |
      DELETE FROM employee_followers WHERE employee_id = 'employee-demo-001' AND user_id = 'user-disp';
      DROP TABLE IF EXISTS employee_followers;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
