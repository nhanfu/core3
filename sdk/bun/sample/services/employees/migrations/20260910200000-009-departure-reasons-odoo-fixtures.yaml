version: 0.0.9
kind: data
type:
  postgres:
    up: |
      DELETE FROM employee_departure_reasons WHERE id IN (
        'departure-reason-resignation',
        'departure-reason-end-contract',
        'departure-reason-retirement',
        'departure-reason-dismissal',
        'departure-reason-relocation'
      );
      INSERT OR IGNORE INTO employee_departure_reasons(id, sequence, name, country_code, row_version) VALUES
        ('departure-reason-fired', 0, 'Fired', NULL, 1),
        ('departure-reason-resigned', 1, 'Resigned', NULL, 1),
        ('departure-reason-retired', 2, 'Retired', NULL, 1);
    down: |
      DELETE FROM employee_departure_reasons WHERE id IN (
        'departure-reason-fired',
        'departure-reason-resigned',
        'departure-reason-retired'
      );
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
