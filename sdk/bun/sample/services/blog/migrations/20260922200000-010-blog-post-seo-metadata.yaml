version: 0.0.10
kind: schema
description: Durable Odoo blog.post SEO metadata
type:
  postgres:
    up: |
      ALTER TABLE blog_posts ADD COLUMN IF NOT EXISTS website_meta_title VARCHAR;
      ALTER TABLE blog_posts ADD COLUMN IF NOT EXISTS website_meta_description VARCHAR;
      ALTER TABLE blog_posts ADD COLUMN IF NOT EXISTS website_meta_keywords VARCHAR;
      ALTER TABLE blog_posts ADD COLUMN IF NOT EXISTS website_meta_og_img VARCHAR;
    down: |
      ALTER TABLE blog_posts DROP COLUMN IF EXISTS website_meta_og_img;
      ALTER TABLE blog_posts DROP COLUMN IF EXISTS website_meta_keywords;
      ALTER TABLE blog_posts DROP COLUMN IF EXISTS website_meta_description;
      ALTER TABLE blog_posts DROP COLUMN IF EXISTS website_meta_title;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
