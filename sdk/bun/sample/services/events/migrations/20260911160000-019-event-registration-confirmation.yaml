version: 0.0.19
kind: data
type:
  postgres:
    up: |
      INSERT INTO event_registrations(id, event_id, event_name, attendee_name, attendee_email, attendee_phone, company_name, booked_by, ticket_type, state, registered_at, row_version)
      VALUES ('registration-demo-unconfirmed', 'event-demo-008', 'An unpublished event', 'Unconfirmed Guest', 'unconfirmed.guest@example.com', '+1 202 555 0199', '', 'My Company (San Francisco)', 'General Admission', 'Unconfirmed', TIMESTAMP '2026-01-15 09:00:00', 1)
      ON CONFLICT (id) DO NOTHING;
    down: |
      DELETE FROM event_registrations WHERE id = 'registration-demo-unconfirmed';
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
