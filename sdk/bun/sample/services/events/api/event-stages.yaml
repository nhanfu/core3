page: { id: event-stages }
datasources:
  - id: event_stages
    permission: events.read
    query: "SELECT id, name, sequence, fold FROM event_stages WHERE (:q IS NULL OR name ILIKE '%' || :q || '%') ORDER BY sequence"
