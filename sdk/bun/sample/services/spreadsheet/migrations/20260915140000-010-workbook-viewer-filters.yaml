version: 0.0.10
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE spreadsheet_workbook_viewer_filters (
        workbook_id VARCHAR NOT NULL, user_id VARCHAR NOT NULL,
        filter_values VARCHAR NOT NULL, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY (workbook_id, user_id)
      );
    down: |
      DROP TABLE spreadsheet_workbook_viewer_filters;
  duckdb: postgres
