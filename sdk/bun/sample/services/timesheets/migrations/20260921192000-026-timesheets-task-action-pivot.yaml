version: 0.0.26
kind: schema
type:
  postgres:
    up: |
      CREATE INDEX IF NOT EXISTS timesheet_entries_task_pivot_idx ON timesheet_entries(task_id, company_name, employee_name, work_date, unit_amount);
    down: |
      DROP INDEX IF EXISTS timesheet_entries_task_pivot_idx;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
