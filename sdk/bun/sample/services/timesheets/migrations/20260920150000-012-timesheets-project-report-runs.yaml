version: 0.0.12
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS timesheet_project_report_runs (
        id VARCHAR PRIMARY KEY,
        project_id VARCHAR NOT NULL,
        project_name VARCHAR NOT NULL,
        requested_by VARCHAR NOT NULL,
        company_name VARCHAR NOT NULL,
        entry_count INTEGER NOT NULL DEFAULT 0,
        total_hours DECIMAL(18,3) NOT NULL DEFAULT 0,
        generated_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00',
        row_version BIGINT NOT NULL DEFAULT 1
      );
      CREATE INDEX IF NOT EXISTS timesheet_project_report_runs_scope_idx ON timesheet_project_report_runs(company_name, project_id, generated_at);
      INSERT OR IGNORE INTO timesheet_project_report_runs(
        id, project_id, project_name, requested_by, company_name,
        entry_count, total_hours, generated_at, row_version
      ) VALUES (
        'timesheet-project-report-run-demo-001', 'project-demo-001',
        'Core3 Implementation', 'Admin User', 'Core3 Demo Company',
        8, 33, TIMESTAMP '2026-01-15 00:00:00', 1
      );
    down: |
      DROP TABLE IF EXISTS timesheet_project_report_runs;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
