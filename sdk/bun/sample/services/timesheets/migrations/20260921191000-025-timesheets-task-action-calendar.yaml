version: 0.0.25
kind: schema
type:
  postgres:
    up: |
      CREATE INDEX IF NOT EXISTS timesheet_entries_task_calendar_idx ON timesheet_entries(task_id, company_name, work_date, employee_name);
    down: |
      DROP INDEX IF EXISTS timesheet_entries_task_calendar_idx;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
