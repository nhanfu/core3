version: 0.0.24
kind: schema
type:
  postgres:
    up: |
      CREATE INDEX IF NOT EXISTS timesheet_entries_task_kanban_idx ON timesheet_entries(task_id, company_name, employee_name, state, work_date);
    down: |
      DROP INDEX IF EXISTS timesheet_entries_task_kanban_idx;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
