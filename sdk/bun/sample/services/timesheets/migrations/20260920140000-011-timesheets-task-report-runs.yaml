version: 0.0.11
kind: schema
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS timesheet_task_report_runs (
        id VARCHAR PRIMARY KEY,
        task_id VARCHAR NOT NULL,
        task_name VARCHAR NOT NULL,
        project_id VARCHAR,
        project_name VARCHAR,
        requested_by VARCHAR NOT NULL,
        company_name VARCHAR NOT NULL,
        entry_count INTEGER NOT NULL DEFAULT 0,
        total_hours DECIMAL(18,3) NOT NULL DEFAULT 0,
        generated_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00',
        row_version BIGINT NOT NULL DEFAULT 1
      );
      CREATE INDEX IF NOT EXISTS timesheet_task_report_runs_scope_idx ON timesheet_task_report_runs(company_name, task_id, generated_at);
      INSERT OR IGNORE INTO timesheet_task_report_runs(
        id, task_id, task_name, project_id, project_name, requested_by, company_name,
        entry_count, total_hours, generated_at, row_version
      ) VALUES (
        'timesheet-task-report-run-demo-001', 'task-demo-001', 'Complete module migration',
        'project-demo-001', 'Core3 Implementation', 'Admin User', 'Core3 Demo Company',
        1, 8, TIMESTAMP '2026-01-15 00:00:00', 1
      );
    down: |
      DROP TABLE IF EXISTS timesheet_task_report_runs;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
