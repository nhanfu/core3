version: 0.0.2
kind: data
type:
  postgres:
    up: |
      INSERT OR IGNORE INTO chat_users(id, email, name, enabled) VALUES
        ('user-admin', 'admin@tms.local', 'Admin User', true),
        ('user-disp', 'dispatcher@tms.local', 'Dispatcher User', true),
        ('user-ops', 'operations@tms.local', 'Operations User', true),
        ('user-fleet', 'fleet@tms.local', 'Fleet User', true);

    down: |
      DELETE FROM chat_users WHERE id IN ('user-admin', 'user-disp', 'user-ops', 'user-fleet');
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
