version: 0.0.2
kind: data
type:
  postgres:
    up: |
      INSERT OR IGNORE INTO chat_users(id, email, name, enabled) VALUES
        ('user-admin', 'admin@tms.local', 'Admin User', true),
        ('user-disp', 'dispatcher@tms.local', 'Dispatcher User', true);

    down: |
      DELETE FROM chat_users WHERE id IN ('user-admin', 'user-disp');
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
