id: chat
kind: domain-service
migrations: migrations
permissions: permissions.yaml
topics: topics.yaml
events: events.yaml
storage: storage.yaml
menu:
  chat:
    path: /chat
    label: Discuss
    icon: message
    permission: chat.read
  groups:
    - id: workspace
      label: Workspace
      items:
        - { path: /chat, label: Discuss, icon: message, permission: chat.read }
        - { path: /chat/channels, label: Channels, icon: hashtag, permission: chat.read }
    - id: configuration
      label: Configuration
      items:
        - { path: /chat/notifications, label: Notifications, icon: bell, permission: chat.read }
        - { path: /chat/voice-video, label: Voice & Video, icon: video, permission: chat.read }
        - { path: /chat/canned-responses, label: Canned Responses, icon: message, permission: chat.read }
        - { path: /chat/roles, label: Roles, icon: users, permission: chat.read }
