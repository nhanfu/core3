page: { id: payment-methods }
datasources:
  - id: accounting_payment_methods
    permission: accounting.read
    query: "SELECT id, row_version, name, description, state FROM accounting_config_payment_methods WHERE (:q IS NULL OR name ILIKE '%' || :q || '%' OR description ILIKE '%' || :q || '%') ORDER BY name"
