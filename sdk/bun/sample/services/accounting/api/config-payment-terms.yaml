page: { id: payment-terms }
datasources:
  - id: accounting_payment_terms
    permission: accounting.read
    query: "SELECT id, row_version, name, description, state FROM accounting_config_payment_terms WHERE (:q IS NULL OR name ILIKE '%' || :q || '%' OR description ILIKE '%' || :q || '%') ORDER BY name"
