version: 0.0.33
description: Accounting cash register action fixtures
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS accounting_cash_registers (id VARCHAR PRIMARY KEY, row_version BIGINT NOT NULL DEFAULT 1, sequence INTEGER NOT NULL DEFAULT 10, reference VARCHAR NOT NULL, date DATE NOT NULL, journal VARCHAR NOT NULL DEFAULT 'Cash', company VARCHAR NOT NULL DEFAULT 'My Company (San Francisco)', currency VARCHAR NOT NULL DEFAULT 'USD', starting_balance DECIMAL(18,2) NOT NULL DEFAULT 0, ending_balance DECIMAL(18,2) NOT NULL DEFAULT 0, line_numbers VARCHAR NOT NULL DEFAULT '', is_complete BOOLEAN NOT NULL DEFAULT TRUE, is_valid BOOLEAN NOT NULL DEFAULT TRUE, problem_description VARCHAR NOT NULL DEFAULT '', attachment_name VARCHAR, attachment_type VARCHAR, attachment_size VARCHAR);
      INSERT INTO accounting_cash_registers (id,sequence,reference,date,journal,starting_balance,ending_balance,line_numbers) VALUES ('accounting-cash-register-001',10,'Cash Register - 2026-08-11',DATE '2026-08-11','Cash',1200,1485,'CASH/2026/00001'),('accounting-cash-register-002',20,'Cash Register - 2026-08-10',DATE '2026-08-10','Cash Furn. Shop',800,800,'') ON CONFLICT (id) DO NOTHING;
    down: DROP TABLE IF EXISTS accounting_cash_registers;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
permissions: [accounting.read, accounting.write, accounting.manage]
