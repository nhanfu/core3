version: 0.0.16
description: Employee Expenses Accounting action fixtures
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS accounting_employee_expenses (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        name VARCHAR NOT NULL,
        employee_name VARCHAR NOT NULL,
        category VARCHAR NOT NULL,
        expense_date DATE NOT NULL,
        amount DECIMAL(18,2) NOT NULL DEFAULT 0,
        payment_method VARCHAR NOT NULL DEFAULT 'Employee',
        description VARCHAR,
        receipt_reference VARCHAR,
        state VARCHAR NOT NULL DEFAULT 'Draft',
        company_name VARCHAR NOT NULL DEFAULT 'My Company (San Francisco)',
        created_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 00:00:00'
      );
      INSERT INTO accounting_employee_expenses (id, row_version, name, employee_name, category, expense_date, amount, payment_method, description, receipt_reference, state, company_name, created_at)
      VALUES
        ('accounting-employee-expense-demo-001', 1, 'Transportation', 'Ronnie Hart', '[TRANS & ACC] Travel & Accommodation', DATE '2026-01-15', 240.00, 'Employee', 'Transportation reimbursement', 'receipt-transportation.pdf', 'Approved', 'My Company (San Francisco)', TIMESTAMP '2026-01-15 00:00:00'),
        ('accounting-employee-expense-demo-002', 1, 'Restaurant', 'Ronnie Hart', '[FOOD] Meals', DATE '2026-01-15', 320.00, 'Employee', 'Restaurant reimbursement', 'receipt-restaurant.pdf', 'Approved', 'My Company (San Francisco)', TIMESTAMP '2026-01-15 00:00:00'),
        ('accounting-employee-expense-demo-003', 1, 'Paintball', 'Keith Byrd', '[EXP_GEN] Expenses', DATE '2026-01-15', 25.00, 'Employee', 'Team activity reimbursement', 'receipt-paintball.pdf', 'Approved', 'My Company (San Francisco)', TIMESTAMP '2026-01-15 00:00:00'),
        ('accounting-employee-expense-demo-004', 1, 'Lunch with Customer', 'Marc Demo', '[EXP_GEN] Expenses', DATE '2026-01-15', 152.80, 'Employee', 'Customer lunch reimbursement', 'receipt-lunch-customer.pdf', 'Approved', 'My Company (San Francisco)', TIMESTAMP '2026-01-15 00:00:00')
      ON CONFLICT (id) DO NOTHING;
    down: |
      DROP TABLE IF EXISTS accounting_employee_expenses;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
