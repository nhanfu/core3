description: Durable Website cookie consent settings
version: 0.0.19
type:
  postgres:
    up: |
      ALTER TABLE website_websites ADD COLUMN IF NOT EXISTS block_third_party_domains BOOLEAN;
      UPDATE website_websites SET block_third_party_domains = TRUE WHERE block_third_party_domains IS NULL;
    down: |
      ALTER TABLE website_websites DROP COLUMN IF EXISTS block_third_party_domains;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
