description: Durable Website Identification favicon uploads
version: 0.0.18
type:
  postgres:
    up: |
      ALTER TABLE website_websites ADD COLUMN IF NOT EXISTS favicon_file_name VARCHAR;
      ALTER TABLE website_websites ADD COLUMN IF NOT EXISTS favicon_mime_type VARCHAR;
      ALTER TABLE website_websites ADD COLUMN IF NOT EXISTS favicon_size_bytes BIGINT;
      ALTER TABLE website_websites ADD COLUMN IF NOT EXISTS favicon_storage_key VARCHAR;
      ALTER TABLE website_websites ADD COLUMN IF NOT EXISTS favicon_content_base64 VARCHAR;
    down: |
      ALTER TABLE website_websites DROP COLUMN IF EXISTS favicon_content_base64;
      ALTER TABLE website_websites DROP COLUMN IF EXISTS favicon_storage_key;
      ALTER TABLE website_websites DROP COLUMN IF EXISTS favicon_size_bytes;
      ALTER TABLE website_websites DROP COLUMN IF EXISTS favicon_mime_type;
      ALTER TABLE website_websites DROP COLUMN IF EXISTS favicon_file_name;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
