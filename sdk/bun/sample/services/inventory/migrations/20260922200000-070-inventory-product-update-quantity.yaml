version: 0.0.70
kind: data
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS inventory_product_quantity_updates (
        id VARCHAR PRIMARY KEY, row_version BIGINT NOT NULL DEFAULT 1,
        product_id VARCHAR NOT NULL, product_name VARCHAR NOT NULL,
        quant_id VARCHAR NOT NULL, location_name VARCHAR NOT NULL,
        previous_quantity DECIMAL(18,3) NOT NULL,
        new_quantity DECIMAL(18,3) NOT NULL,
        quantity_delta DECIMAL(18,3) NOT NULL,
        company_name VARCHAR NOT NULL, updated_by VARCHAR NOT NULL,
        updated_at TIMESTAMP NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_inventory_product_quantity_updates_context
        ON inventory_product_quantity_updates(product_id, company_name, updated_at, id);
      INSERT INTO inventory_product_quantity_updates
        (id, product_id, product_name, quant_id, location_name, previous_quantity,
         new_quantity, quantity_delta, company_name, updated_by, updated_at)
      VALUES
        ('product-quantity-update-inventory-template-storage-box-1',
         'inventory-template-storage-box', 'Storage Box', 'quant-box-main',
         'Stock', 18, 18, 0, 'Core3 Demo Company', 'Seeded Inventory',
         TIMESTAMP '2026-01-15 08:30:00')
      ON CONFLICT(id) DO NOTHING;
    down: |
      DELETE FROM inventory_product_quantity_updates
        WHERE id = 'product-quantity-update-inventory-template-storage-box-1';
      DROP TABLE IF EXISTS inventory_product_quantity_updates;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
