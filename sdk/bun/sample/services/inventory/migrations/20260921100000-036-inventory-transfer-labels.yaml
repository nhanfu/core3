version: 0.0.36
kind: data
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS inventory_transfer_label_runs (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        picking_id VARCHAR NOT NULL,
        company_name VARCHAR NOT NULL,
        label_type VARCHAR NOT NULL,
        output_format VARCHAR NOT NULL,
        label_count DECIMAL(18,3) NOT NULL,
        requested_by VARCHAR NOT NULL,
        created_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 08:00:00',
        UNIQUE(picking_id, id)
      );
      CREATE INDEX IF NOT EXISTS inventory_transfer_label_runs_picking_idx
        ON inventory_transfer_label_runs(picking_id, created_at, id);
      INSERT INTO inventory_pickings(id, operation_type_id, name, contact_name, scheduled_date, source_document, company_name, source_location_id, destination_location_id, state)
      VALUES ('delivery-labels-0001', 'operation-deliveries', 'WH/OUT/LABEL/0001', 'Core3 Demo Customer', DATE '2026-01-15', 'SO/LABEL/0001', 'Core3 Demo Company', 'location-stock', 'location-customer', 'Ready')
      ON CONFLICT(id) DO NOTHING;
      INSERT INTO inventory_picking_moves(id, picking_id, product_name, quantity, done_quantity)
      VALUES ('delivery-labels-0001-move-001', 'delivery-labels-0001', '[E-COM08] Storage Box', 3, 0)
      ON CONFLICT(id) DO NOTHING;
    down: |
      DELETE FROM inventory_transfer_label_runs WHERE picking_id = 'delivery-labels-0001';
      DELETE FROM inventory_transfer_messages WHERE picking_id = 'delivery-labels-0001';
      DELETE FROM inventory_picking_moves WHERE picking_id = 'delivery-labels-0001';
      DELETE FROM inventory_pickings WHERE id = 'delivery-labels-0001';
      DROP TABLE IF EXISTS inventory_transfer_label_runs;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
