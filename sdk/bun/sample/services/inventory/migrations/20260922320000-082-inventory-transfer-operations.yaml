version: 0.0.82
description: Inventory transfer Operations stat fixture
type:
  postgres:
    up: |
      INSERT INTO inventory_pickings(id, operation_type_id, name, contact_name, scheduled_date, source_document, company_name, source_location_id, destination_location_id, state, row_version)
      VALUES ('delivery-operations-0001', 'operation-deliveries', 'WH/OUT/OPERATIONS/0001', 'Operations Customer', DATE '2026-01-15', 'SO/OPERATIONS/0001', 'Core3 Demo Company', 'location-stock', 'location-customer', 'Ready', 1)
      ON CONFLICT(id) DO NOTHING;
      INSERT INTO inventory_picking_moves(id, picking_id, product_name, quantity, done_quantity)
      VALUES
        ('delivery-operations-0001-move-001', 'delivery-operations-0001', '[E-COM08] Storage Box', 4, 0),
        ('delivery-operations-0001-move-002', 'delivery-operations-0001', '[FURN_8855] Drawer', 2, 1)
      ON CONFLICT(id) DO NOTHING;
    down: |
      DELETE FROM inventory_picking_moves WHERE id IN ('delivery-operations-0001-move-001', 'delivery-operations-0001-move-002');
      DELETE FROM inventory_pickings WHERE id = 'delivery-operations-0001';
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
