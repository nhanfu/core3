version: 0.0.20
kind: data
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS inventory_package_move_lines (
        id VARCHAR PRIMARY KEY,
        package_id VARCHAR NOT NULL,
        move_id VARCHAR NOT NULL,
        relation_type VARCHAR NOT NULL,
        created_at TIMESTAMP NOT NULL DEFAULT TIMESTAMP '2026-01-15 08:00:00'
      );
      INSERT INTO inventory_package_move_lines(id, package_id, move_id, relation_type) VALUES
        ('package-move-0001', 'package-main-0001', 'receipt-00001-move-001', 'result'),
        ('package-move-0002', 'package-main-0001', 'delivery-00001-move-001', 'source'),
        ('package-move-0003', 'package-box-0002', 'delivery-00001-move-002', 'source'),
        ('package-move-0004', 'package-customer-0003', 'delivery-00001-move-001', 'result')
      ON CONFLICT(id) DO NOTHING;
    down: |
      DELETE FROM inventory_package_move_lines WHERE id IN (
        'package-move-0001', 'package-move-0002', 'package-move-0003', 'package-move-0004'
      );
      DROP TABLE IF EXISTS inventory_package_move_lines;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
