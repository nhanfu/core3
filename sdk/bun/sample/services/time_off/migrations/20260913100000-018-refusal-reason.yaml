version: 0.0.18
kind: data
type:
  postgres:
    up: |
      ALTER TABLE leave_requests ADD COLUMN IF NOT EXISTS refusal_reason VARCHAR;
    down: |
      ALTER TABLE leave_requests DROP COLUMN IF EXISTS refusal_reason;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
