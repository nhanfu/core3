version: 0.0.69
description: Durable Odoo Survey suggested-value create fixture
kind: data
type:
  postgres:
    up: |
      INSERT INTO surveys(id, name, title, description, owner, response_count, state, access_token, row_version, created_at, updated_at)
      VALUES ('survey-demo-suggested-value-create', 'SURVEY/SUGGESTED-VALUE-CREATE', 'Suggested Value Create Survey', 'A deterministic choice question used for suggested-value creation.',  'Mitchell Admin', 0, 'Draft', 'suggested-value-create-token-2026', 1, TIMESTAMP '2026-01-15 09:00:00', TIMESTAMP '2026-01-15 09:00:00')
      ON CONFLICT (id) DO UPDATE SET state = 'Draft', row_version = 1, updated_at = TIMESTAMP '2026-01-15 09:00:00';
      INSERT INTO survey_questions(id, survey_id, question_text, question_type, sequence, required, answer_options, row_version, updated_at)
      VALUES ('question-suggested-value-create', 'survey-demo-suggested-value-create', 'Which onboarding path should we improve?', 'Choice', 1, true, 'Guided,Self service', 1, TIMESTAMP '2026-01-15 09:00:00')
      ON CONFLICT (id) DO UPDATE SET survey_id = excluded.survey_id, question_text = excluded.question_text, question_type = excluded.question_type, sequence = excluded.sequence, required = excluded.required, answer_options = excluded.answer_options, row_version = 1, updated_at = TIMESTAMP '2026-01-15 09:00:00';
      DELETE FROM survey_suggested_values WHERE id = 'answer-suggested-value-create-seeded';
      INSERT INTO survey_suggested_values(id, row_version, question_id, question_text, value, sequence, score, updated_at)
      VALUES ('answer-suggested-value-create-seeded', 1, 'question-suggested-value-create', 'Which onboarding path should we improve?', 'Guided', 1, 0, TIMESTAMP '2026-01-15 09:00:00')
      ON CONFLICT (id) DO UPDATE SET question_id = excluded.question_id, question_text = excluded.question_text, value = excluded.value, sequence = excluded.sequence, score = excluded.score, row_version = 1, updated_at = TIMESTAMP '2026-01-15 09:00:00';
    down: |
      DELETE FROM survey_suggested_values WHERE question_id = 'question-suggested-value-create';
      DELETE FROM survey_questions WHERE id = 'question-suggested-value-create';
      DELETE FROM surveys WHERE id = 'survey-demo-suggested-value-create';
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
