page: { id: survey-suggested-values }
datasources:
  - id: survey_suggested_values
    permission: surveys.read
    query: "SELECT id, row_version, question_text, value, sequence, score, matrix_row, matrix_column FROM survey_suggested_values WHERE (:q IS NULL OR question_text ILIKE '%' || :q || '%' OR value ILIKE '%' || :q || '%') ORDER BY question_text, sequence, id"
