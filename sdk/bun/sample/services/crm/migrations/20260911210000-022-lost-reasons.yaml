version: 0.0.22
type:
  postgres:
    up: |
      ALTER TABLE crm_lost_reasons ADD COLUMN IF NOT EXISTS row_version BIGINT;
      UPDATE crm_lost_reasons SET row_version = 1 WHERE row_version IS NULL;
      UPDATE crm_lost_reasons SET active = false WHERE id IN ('crm-lost-budget', 'crm-lost-timing');
      INSERT INTO crm_lost_reasons(id, name, active, row_version) VALUES
        ('crm-lost-expensive', 'Too expensive', true, 1),
        ('crm-lost-skills', 'We don''t have people/skills', true, 1),
        ('crm-lost-stock', 'Not enough stock', true, 1),
        ('crm-lost-legacy', 'Legacy no decision', false, 1)
      ON CONFLICT(id) DO NOTHING;
    down: |
      DELETE FROM crm_lost_reasons WHERE id IN ('crm-lost-expensive', 'crm-lost-skills', 'crm-lost-stock', 'crm-lost-legacy');
      ALTER TABLE crm_lost_reasons DROP COLUMN IF EXISTS row_version;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
