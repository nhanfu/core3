version: 0.0.24
type:
  postgres:
    up: |
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS sequence INTEGER;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS summary VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS delay_count INTEGER;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS delay_unit VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS delay_from VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS res_model VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS category VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS default_user_id VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS chaining_type VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS triggered_next_type VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS suggested_next_types VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS default_note VARCHAR;
      ALTER TABLE crm_activity_types ADD COLUMN IF NOT EXISTS row_version INTEGER;
      UPDATE crm_activity_types SET sequence = COALESCE(sequence, 10), delay_count = COALESCE(delay_count, 0), delay_unit = COALESCE(delay_unit, 'days'), delay_from = COALESCE(delay_from, 'previous_activity'), category = COALESCE(category, 'default'), default_user_id = COALESCE(default_user_id, 'Salesperson'), chaining_type = COALESCE(chaining_type, 'suggest'), row_version = COALESCE(row_version, 1);
      UPDATE crm_activity_types SET sequence = CASE name WHEN 'Call' THEN 1 WHEN 'Meeting' THEN 2 WHEN 'Email' THEN 3 WHEN 'To-Do' THEN 4 ELSE sequence END,
        summary = CASE name WHEN 'Call' THEN 'Discuss next steps' WHEN 'Meeting' THEN 'Prepare meeting' WHEN 'Email' THEN 'Send a follow-up' WHEN 'To-Do' THEN 'Complete a task' ELSE summary END,
        delay_count = CASE name WHEN 'Call' THEN 2 WHEN 'Meeting' THEN 5 WHEN 'Email' THEN 1 ELSE delay_count END,
        delay_unit = CASE name WHEN 'Call' THEN 'days' WHEN 'Meeting' THEN 'days' WHEN 'Email' THEN 'days' ELSE delay_unit END,
        delay_from = CASE name WHEN 'Call' THEN 'current_date' WHEN 'Meeting' THEN 'current_date' WHEN 'Email' THEN 'previous_activity' ELSE delay_from END,
        res_model = CASE name WHEN 'Call' THEN NULL WHEN 'Meeting' THEN 'res.partner' WHEN 'Email' THEN 'res.partner' WHEN 'To-Do' THEN NULL ELSE res_model END
      WHERE name IN ('Call', 'Meeting', 'Email', 'To-Do');
    down: |
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS category;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS default_user_id;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS chaining_type;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS triggered_next_type;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS suggested_next_types;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS default_note;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS res_model;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS delay_from;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS delay_unit;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS delay_count;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS summary;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS sequence;
      ALTER TABLE crm_activity_types DROP COLUMN IF EXISTS row_version;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
