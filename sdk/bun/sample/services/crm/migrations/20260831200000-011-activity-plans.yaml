version: 0.0.11

type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS crm_activity_plans (
        id VARCHAR PRIMARY KEY DEFAULT gen_random_uuid(), name VARCHAR NOT NULL UNIQUE,
        active BOOLEAN NOT NULL DEFAULT true, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
      );
      CREATE TABLE IF NOT EXISTS crm_activity_plan_steps (
        id VARCHAR PRIMARY KEY DEFAULT gen_random_uuid(), plan_id VARCHAR NOT NULL REFERENCES crm_activity_plans(id),
        sequence INTEGER NOT NULL, activity_type VARCHAR NOT NULL, summary VARCHAR NOT NULL,
        delay_days INTEGER NOT NULL DEFAULT 0, active BOOLEAN NOT NULL DEFAULT true
      );
      INSERT INTO crm_activity_plans(id, name) VALUES ('crm-plan-new-opportunity', 'New opportunity follow-up') ON CONFLICT (id) DO NOTHING;
      INSERT INTO crm_activity_plan_steps(id, plan_id, sequence, activity_type, summary, delay_days) VALUES
        ('crm-plan-new-opportunity-call', 'crm-plan-new-opportunity', 10, 'Call', 'Qualify the opportunity', 2),
        ('crm-plan-new-opportunity-meeting', 'crm-plan-new-opportunity', 20, 'Meeting', 'Review the proposed solution', 7)
        ON CONFLICT (id) DO NOTHING;
    down: |
      DROP TABLE IF EXISTS crm_activity_plan_steps;
      DROP TABLE IF EXISTS crm_activity_plans;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
