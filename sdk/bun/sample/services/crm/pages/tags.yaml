title: CRM Lead Tags
page:
  id: crm-tags
  breadcrumb: [CRM, Tags]
  auth: { require: [crm.read] }
components:
  - type: ListView
    variant: odoo
    source: crm_tag_values
    search: { label: Search tags, placeholder: Search lead tags... }
    columns:
      - { field: value, label: Tag }
      - { field: lead_count, label: Leads }
  - type: ListView
    variant: odoo
    source: crm_tag_configuration
    create_action: create_crm_tag
    create_label: New tag
    columns:
      - { field: name, label: Tag }
      - { field: lead_count, label: Leads }
      - { field: color, label: Color }
      - { field: active, label: Active, type: StatusChip }
    row_key: id
    selectable: true
    bulk_actions:
      - { id: bulk_delete_crm_tags, label: Delete selected, permission: crm.manage }
    row_actions: menu
    actions:
      - { id: edit_crm_tag, label: Edit, permission: crm.manage, variant: ghost }
      - { id: toggle_crm_tag, label: Toggle, permission: crm.manage, variant: ghost }
