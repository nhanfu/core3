version: 0.0.9
kind: schema
type:
  postgres:
    up: |
      ALTER TABLE base_states ADD COLUMN IF NOT EXISTS row_version BIGINT;
      UPDATE base_states SET row_version = 1 WHERE row_version IS NULL;
    down: |
      ALTER TABLE base_states DROP COLUMN IF EXISTS row_version;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
