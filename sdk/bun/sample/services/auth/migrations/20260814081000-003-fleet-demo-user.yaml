kind: data
up: |
  INSERT OR IGNORE INTO users(id, email, name, password_hash, preferred_lang, branch_id, department_id)
  VALUES ('user-fleet', 'fleet@tms.local', 'Fleet Manager', 'fleet123', 'en', 'branch-hcm', 'department-02');
  INSERT OR IGNORE INTO user_roles(user_id, role_id)
  VALUES ('user-fleet', 'role-dispatcher');
down: |
  DELETE FROM user_roles WHERE user_id = 'user-fleet';
  DELETE FROM users WHERE id = 'user-fleet';
