version: 0.0.34
kind: data
description: Seed durable Purchase note-line support
type:
  postgres:
    up: |
      INSERT OR IGNORE INTO purchase_order_lines(id, order_id, product_id, row_version, sequence, display_type, product_name, description, quantity, qty_received, qty_billed, uom, unit_price, tax_rate, line_total)
      VALUES ('purchase-note-demo-008-20', 'po-demo-008', NULL, 1, 20, 'line_note', NULL, 'Confirm delivery window with the vendor', 0, 0, 0, '', 0, 0, 0);
    down: |
      DELETE FROM purchase_order_lines WHERE id = 'purchase-note-demo-008-20';
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
