version: 0.0.18
type:
  postgres:
    up: |
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS cost DECIMAL(18,2);
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS forecasted DECIMAL(18,2);
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS product_type VARCHAR;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS invoicing_policy VARCHAR;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS track_inventory BOOLEAN;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS sales_taxes VARCHAR;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS purchase_taxes VARCHAR;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS category VARCHAR;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS company_name VARCHAR;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS internal_notes VARCHAR;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS sales BOOLEAN;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS pos_available BOOLEAN;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS expenses BOOLEAN;
      ALTER TABLE purchase_product_variants ADD COLUMN IF NOT EXISTS purchase BOOLEAN;
      CREATE TABLE IF NOT EXISTS purchase_product_variant_messages (
        id VARCHAR PRIMARY KEY, variant_id VARCHAR NOT NULL, actor_name VARCHAR NOT NULL,
        action VARCHAR NOT NULL, action_label VARCHAR NOT NULL, detail VARCHAR NOT NULL,
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
      );
      INSERT OR IGNORE INTO purchase_product_variant_messages(id, variant_id, actor_name, action, action_label, detail, created_at)
      SELECT 'purchase-variant-message-acoustic-created', id, 'OdooBot', 'created', 'Product Variant created', 'Product Variant created', TIMESTAMP '2026-09-10 06:56:00' FROM purchase_product_variants WHERE product_id = 'purchase-product-acoustic';
      INSERT OR IGNORE INTO purchase_product_variant_messages(id, variant_id, actor_name, action, action_label, detail, created_at)
      SELECT 'purchase-variant-message-acoustic-track', id, 'OdooBot', 'purchase.product_variants.note', 'Note', 'NoYes (Track Inventory)', TIMESTAMP '2026-09-10 06:58:00' FROM purchase_product_variants WHERE product_id = 'purchase-product-acoustic';
      INSERT OR IGNORE INTO purchase_product_variant_messages(id, variant_id, actor_name, action, action_label, detail, created_at)
      SELECT 'purchase-variant-message-acoustic-quantities', id, 'OdooBot', 'purchase.product_variants.note', 'Note', 'Ordered quantitiesDelivered quantities (Invoicing Policy)', TIMESTAMP '2026-09-10 07:00:00' FROM purchase_product_variants WHERE product_id = 'purchase-product-acoustic';
    down: |
      DROP TABLE IF EXISTS purchase_product_variant_messages;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS purchase;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS expenses;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS pos_available;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS sales;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS internal_notes;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS company_name;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS category;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS purchase_taxes;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS sales_taxes;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS track_inventory;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS invoicing_policy;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS product_type;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS forecasted;
      ALTER TABLE purchase_product_variants DROP COLUMN IF EXISTS cost;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
