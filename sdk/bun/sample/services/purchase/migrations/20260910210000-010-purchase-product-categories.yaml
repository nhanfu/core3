version: 0.0.10
kind: data
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS purchase_product_categories (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        name VARCHAR NOT NULL,
        parent_name VARCHAR NOT NULL DEFAULT '',
        force_removal_strategy VARCHAR NOT NULL DEFAULT '',
        packaging_reserve_method VARCHAR NOT NULL DEFAULT 'partial',
        costing_method VARCHAR NOT NULL DEFAULT 'Standard Price',
        inventory_valuation VARCHAR NOT NULL DEFAULT 'Periodic (at closing)',
        product_count INTEGER NOT NULL DEFAULT 0,
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
      );
      INSERT OR IGNORE INTO purchase_product_categories
        (id, name, parent_name, force_removal_strategy, packaging_reserve_method, costing_method, inventory_valuation, product_count)
      VALUES
        ('purchase-category-clothes', 'Clothes', '', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-expenses', 'Expenses', '', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-food', 'Food', '', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-furniture', 'Furniture', '', '', 'partial', 'Standard Price', 'Periodic (at closing)', 30),
        ('purchase-category-furniture-office', 'Furniture / Office', 'Furniture', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-furniture-outdoor', 'Furniture / Outdoor', 'Furniture', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-goods', 'Goods', '', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-home-construction', 'Home Construction', '', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-services', 'Services', '', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0),
        ('purchase-category-services-events', 'Services / Events', 'Services', '', 'partial', 'Standard Price', 'Periodic (at closing)', 0);
      CREATE TABLE IF NOT EXISTS purchase_product_category_messages (
        id VARCHAR PRIMARY KEY,
        category_id VARCHAR NOT NULL,
        actor_name VARCHAR NOT NULL,
        action VARCHAR NOT NULL,
        action_label VARCHAR NOT NULL,
        detail VARCHAR NOT NULL DEFAULT '',
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
      );
      INSERT OR IGNORE INTO purchase_product_category_messages
        (id, category_id, actor_name, action, action_label, detail, created_at)
      VALUES
        ('purchase-category-message-furniture-created', 'purchase-category-furniture', 'OdooBot', 'created', 'Product Category created', '', TIMESTAMP '2026-09-08 14:54:00');
    down: |
      DROP TABLE IF EXISTS purchase_product_category_messages;
      DROP TABLE IF EXISTS purchase_product_categories;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
