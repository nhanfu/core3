id: maintenance
kind: domain-service
database: duckdb
migrations: migrations
permissions: permissions.yaml
storage: storage.yaml
menu:
  maintenance:
    path: /maintenance
    label: Maintenance
    icon: wrench
    permission: maintenance.read
  groups:
    - id: maintenance
      label: Maintenance
      items:
        - { path: /maintenance-requests, label: Maintenance Requests, icon: wrench, permission: maintenance.read }
        - { path: /maintenance-calendar, label: Maintenance Calendar, icon: calendar, permission: maintenance.read }
    - id: equipment
      label: Equipment
      items:
        - { path: /equipments, label: Equipment, icon: machine, permission: maintenance.read }
    - id: reporting
      label: Reporting
      items:
        - { path: /maintenance-requests-analysis, label: Maintenance Requests Analysis, icon: chart, permission: maintenance.read }
    - id: configuration
      label: Configuration
      items:
        - { path: /maintenance-teams, label: Maintenance Teams, icon: users, permission: maintenance.manage }
        - { path: /equipement-categories, label: Equipment Categories, icon: category, permission: maintenance.manage }
        - { path: /maintenance-stages, label: Maintenance Stages, icon: pipeline, permission: maintenance.manage }
        - { path: /maintenance-settings, label: Settings, icon: settings, permission: maintenance.settings }
