version: 0.0.66
kind: schema
description: durable eCommerce alternative product recommendations
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS ecommerce_product_alternatives (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        source_product_id VARCHAR NOT NULL,
        destination_product_id VARCHAR NOT NULL,
        sequence INTEGER NOT NULL DEFAULT 10,
        company_name VARCHAR NOT NULL DEFAULT 'My Company',
        active BOOLEAN NOT NULL DEFAULT TRUE,
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(source_product_id, destination_product_id)
      );
      CREATE INDEX IF NOT EXISTS ecommerce_product_alternatives_source_idx
        ON ecommerce_product_alternatives(source_product_id, active, sequence, id);
    down: |
      DROP INDEX IF EXISTS ecommerce_product_alternatives_source_idx;
      DROP TABLE IF EXISTS ecommerce_product_alternatives;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
