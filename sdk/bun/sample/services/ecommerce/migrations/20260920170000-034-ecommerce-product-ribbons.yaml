version: 0.0.34
kind: schema
description: eCommerce product ribbon configuration
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS ecommerce_product_ribbons (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        name VARCHAR NOT NULL,
        sequence INTEGER NOT NULL DEFAULT 10,
        bg_color VARCHAR NOT NULL DEFAULT '#000000',
        text_color VARCHAR NOT NULL DEFAULT '#FFFFFF',
        position VARCHAR NOT NULL DEFAULT 'left',
        style VARCHAR NOT NULL DEFAULT 'ribbon',
        assign VARCHAR NOT NULL DEFAULT 'manual',
        new_period INTEGER NOT NULL DEFAULT 30,
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
      );
    down: DROP TABLE IF EXISTS ecommerce_product_ribbons;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
