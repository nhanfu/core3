version: 0.0.53
kind: data
description: deterministic product tag image fixture migration
type:
  postgres:
    up: |
      -- Image bytes are intentionally uploaded through the attachment contract;
      -- this idempotent data migration keeps the tag fixture set stable.
      INSERT INTO ecommerce_product_tags (id, name, sequence, color, visible_to_customers)
      VALUES ('ecommerce-tag-featured', 'Featured', 1, '#875A7B', TRUE)
      ON CONFLICT (id) DO NOTHING;
    down: |
      DELETE FROM ecommerce_product_tag_images WHERE tag_id = 'ecommerce-tag-featured';
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
