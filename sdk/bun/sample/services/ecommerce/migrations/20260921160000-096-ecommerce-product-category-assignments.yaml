version: 0.0.96
kind: schema
description: durable Website Sale product public category assignments
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS ecommerce_product_category_assignments (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        product_id VARCHAR NOT NULL,
        category_id VARCHAR NOT NULL,
        sequence INTEGER NOT NULL DEFAULT 10,
        active BOOLEAN NOT NULL DEFAULT TRUE,
        company_name VARCHAR,
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
      );
      CREATE UNIQUE INDEX IF NOT EXISTS ecommerce_product_category_assignment_unique_idx ON ecommerce_product_category_assignments(product_id, category_id);
      CREATE INDEX IF NOT EXISTS ecommerce_product_category_assignment_product_idx ON ecommerce_product_category_assignments(product_id, sequence, id);
    down: DROP TABLE IF EXISTS ecommerce_product_category_assignments;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
