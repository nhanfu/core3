version: 0.0.152
kind: schema
description: durable Website Sale Shop product-card Add to Cart CTA visibility policy
type:
  postgres:
    up: |
      CREATE TABLE IF NOT EXISTS ecommerce_shop_product_cta_policies (
        id VARCHAR PRIMARY KEY,
        row_version BIGINT NOT NULL DEFAULT 1,
        company_name VARCHAR NOT NULL,
        show_cta BOOLEAN NOT NULL DEFAULT TRUE,
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(company_name)
      );
      CREATE INDEX IF NOT EXISTS ecommerce_shop_product_cta_company_idx
        ON ecommerce_shop_product_cta_policies(company_name);
    down: |
      DROP INDEX IF EXISTS ecommerce_shop_product_cta_company_idx;
      DROP TABLE IF EXISTS ecommerce_shop_product_cta_policies;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
