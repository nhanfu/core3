id: order
kind: domain-service
database:
  storage:
    driver: duckdb
    path_env: CORE3_ORDER_DB_PATH
  compute:
    driver: duckdb
    mode: memory
migrations: migrations
pages:
  - pages/menu.yaml
  - pages/orders.yaml
  - pages/order-detail.yaml
  - pages/order-workflow.yaml
permissions: permissions.yaml
storage: storage.yaml
