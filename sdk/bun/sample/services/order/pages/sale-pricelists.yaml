title: Pricelists
page: { id: sale-pricelists, route: /order/pricelists, breadcrumb: [Sales, Pricelists], auth: { require: [orders.read] } }
components:
  - type: ListView
    variant: odoo
    source: sale_pricelists
    create_action: create_sale_pricelist
    create_label: New pricelist
    columns:
      - { field: name, label: Pricelist, type: PrimaryEntityCell }
      - { field: currency, label: Currency }
      - { field: discount_policy, label: Discount policy }
      - { field: rule_count, label: Rules, align: right }
      - { field: active, label: Active, type: StatusChip }
    page_size: 50
    row_key: id
    row_double_click_action: view_sale_pricelist
    row_actions: menu
    actions:
      - { id: archive_sale_pricelist, label: Archive, permission: orders.write, variant: danger, show_if: "row.active === true" }
    form_view: { page: apps/services/order/pages/sale-pricelists-detail.yaml, side_panel: true }
