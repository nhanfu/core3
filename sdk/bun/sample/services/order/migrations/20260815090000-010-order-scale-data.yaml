version: 0.1.0
kind: data
description: Deterministic scale dataset for CRUD and pivot verification
generate:
  table: orders
  count: 1000
  columns:
    id: bulk-order-${n}
    order_number: SCALE-${n}
    customer_name: Scale Customer ${n}
    order_date: '2026-01-${mod:28}'
    status: '${cycle:Draft|Pending Approval|Approved|Cancelled}'
    created_by: Scale Seeder
type:
  postgres:
    up: |
      -- Rows are generated by the migration runner.

    down: |
      DELETE FROM orders WHERE id LIKE 'bulk-order-%';
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
