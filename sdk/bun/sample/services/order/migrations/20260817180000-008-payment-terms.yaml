version: 0.0.8
type:
  postgres:
    up: |
      ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_term_name VARCHAR;
      CREATE TABLE IF NOT EXISTS sale_payment_terms (
        id VARCHAR PRIMARY KEY DEFAULT gen_random_uuid(), name VARCHAR NOT NULL UNIQUE,
        due_days INTEGER NOT NULL DEFAULT 0, discount_days INTEGER NOT NULL DEFAULT 0,
        discount_percent DECIMAL(9,4) NOT NULL DEFAULT 0, active BOOLEAN NOT NULL DEFAULT true,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
      );
      INSERT INTO sale_payment_terms(id, name, due_days, discount_days, discount_percent)
      VALUES ('payment-term-immediate', 'Immediate Payment', 0, 0, 0),
             ('payment-term-net30', 'Net 30', 30, 0, 0),
             ('payment-term-net60', 'Net 60', 60, 0, 0)
      ON CONFLICT(name) DO NOTHING;
    down: DROP TABLE IF EXISTS sale_payment_terms;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
