version: 0.0.7
type:
  postgres:
    up: |
      ALTER TABLE customers ADD COLUMN IF NOT EXISTS email VARCHAR;
      ALTER TABLE customers ADD COLUMN IF NOT EXISTS phone VARCHAR;
      ALTER TABLE customers ADD COLUMN IF NOT EXISTS street VARCHAR;
      ALTER TABLE customers ADD COLUMN IF NOT EXISTS city VARCHAR;
      ALTER TABLE customers ADD COLUMN IF NOT EXISTS country VARCHAR;
      ALTER TABLE customers ADD COLUMN IF NOT EXISTS vat VARCHAR;
      ALTER TABLE customers ADD COLUMN IF NOT EXISTS company_type VARCHAR;
    down: |
      ALTER TABLE customers DROP COLUMN IF EXISTS company_type;
      ALTER TABLE customers DROP COLUMN IF EXISTS vat;
      ALTER TABLE customers DROP COLUMN IF EXISTS country;
      ALTER TABLE customers DROP COLUMN IF EXISTS city;
      ALTER TABLE customers DROP COLUMN IF EXISTS street;
      ALTER TABLE customers DROP COLUMN IF EXISTS phone;
      ALTER TABLE customers DROP COLUMN IF EXISTS email;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
