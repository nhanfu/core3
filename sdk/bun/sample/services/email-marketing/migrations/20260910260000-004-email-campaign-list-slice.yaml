version: 0.0.4
kind: schema
type:
  postgres:
    up: |
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS row_version BIGINT;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS active BOOLEAN;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS favorite BOOLEAN;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS is_public BOOLEAN;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS company_name VARCHAR;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS contact_count INTEGER;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS mailing_count INTEGER;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS bounce_count INTEGER;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS optout_count INTEGER;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS blacklist_count INTEGER;
      ALTER TABLE mailing_lists ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS reply_to VARCHAR;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS recipient_model VARCHAR;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS recipient_domain VARCHAR;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS failed_count INTEGER;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS retriable_count INTEGER;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS active BOOLEAN;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS favorite BOOLEAN;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS mail_server_available BOOLEAN;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS is_ab_test BOOLEAN;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS ab_test_parent_id VARCHAR;
      ALTER TABLE email_campaigns ADD COLUMN IF NOT EXISTS winner_id VARCHAR;
      UPDATE mailing_lists SET row_version = COALESCE(row_version, 1), active = CASE WHEN state = 'Archived' THEN false ELSE COALESCE(active, true) END, favorite = COALESCE(favorite, false), is_public = COALESCE(is_public, true), company_name = COALESCE(NULLIF(company_name, ''), 'Core3 Vietnam'), contact_count = COALESCE(contact_count, subscriber_count), mailing_count = COALESCE(mailing_count, 0), bounce_count = COALESCE(bounce_count, 0), optout_count = COALESCE(optout_count, 0), blacklist_count = COALESCE(blacklist_count, 0), updated_at = TIMESTAMP '2026-01-15 09:00:00';
      UPDATE email_campaigns SET row_version = COALESCE(row_version, 1), active = COALESCE(active, true), favorite = COALESCE(favorite, false), recipient_model = COALESCE(NULLIF(recipient_model, ''), 'res.partner'), recipient_domain = COALESCE(NULLIF(recipient_domain, ''), 'Active subscribers'), failed_count = COALESCE(failed_count, 0), retriable_count = COALESCE(retriable_count, 0), mail_server_available = COALESCE(mail_server_available, true), is_ab_test = COALESCE(is_ab_test, false);
    down: |
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS winner_id;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS ab_test_parent_id;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS is_ab_test;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS mail_server_available;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS favorite;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS active;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS retriable_count;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS failed_count;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS recipient_domain;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS recipient_model;
      ALTER TABLE email_campaigns DROP COLUMN IF EXISTS reply_to;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS updated_at;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS blacklist_count;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS optout_count;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS bounce_count;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS mailing_count;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS contact_count;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS company_name;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS is_public;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS favorite;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS active;
      ALTER TABLE mailing_lists DROP COLUMN IF EXISTS row_version;
  duckdb: postgres
  mysql: postgres
  oracle: postgres
  sqlserver: postgres
